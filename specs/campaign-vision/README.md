# Enable campaign vision without gameplay regressions

The user has narrowed the remaining goal: noticeable visual behavior is not required; enable the new vision, check gameplay regressions, then wrap up. Do not pursue a new lighting puzzle, neural tuning, attraction, or extra sensory UI in this closeout. The earlier five-objective plan is superseded to that extent.

## Next Agent Prompt

Implement this single vertical integration. Reuse the validated Tm2 map and existing Source interface. Enable vision and the modeled visible emitters in both current campaign rooms, preserve their other settings, verify actual exported content/browser behavior, then review and archive. Update this handoff after the pass.

- [ ] Shared authored emitter definitions feed renderer and simulation.
- [ ] Both campaign exports enable vision gain3 and resolved sources.
- [ ] Unit, regression, native/browser, pacing and visual gates pass.
- [ ] Review choices, close and report the measured limits.

## One contract and one owner

A small pure shared house-lighting module defines sconce and exit emitters: world position, visual intensity, simulated rate and radius. The two intensities have different meanings even when initially assigned equal values. Quarter-turn mounts and physical exit geometry determine placement; no camera, scene traversal or Three.js state feeds sensing. Renderer light instances and authoring-time LevelDef.sources consume those same definitions. The existing FieldSet remains the only runtime sensing implementation.

Keep the tested settings: sconce mount-local offset (0,.18,.22), visual intensity/source rate .45, radius2.5; exit emitter .55m inward from opening center, height1.15, visual intensity/source rate9, radius4.6. Reuse the existing sconce mount positions and quarter turns. The campaign authoring module owns the current room fixture list; remove duplicate sconce declarations. Base level modules export fully resolved content, so browser and native serialization receive the same sources and tuning. Remove stale comments claiming these lights have no sensory meaning.

Append vision gain3 to both rooms. Preserve ambient1, every original odor/taste setting, geometry, body configuration, timer, population16, placements, source-independent hazards, suction, progress, and2/5/10stars. About remains unchanged. Decorative spill/haze, light colors, falloff artwork and all visual dimensions remain as before. No asset edits, new runtime map, source lookup service, renderer-to-simulation callback or compatibility migration.

Existing Rust examples that parse TypeScript literals must not silently omit resolved lighting when they claim to sample authored fields. Make the sensory performance example accept resolved content JSON. Geometry/body-only fixture tests may retain their deliberately limited base fixtures; they do not claim complete resolved campaign inputs.

## Regression evidence

The preceding full seed42 playtest used16flies, empty placements and original horizons. Old/current vision-off trajectories match exactly. With modeled visible lights and vision enabled, Open Window has3escapes and Turn the Corner2; each earns1star. These are regression fingerprints, not required balance targets or evidence of attraction. See [the preserved comparison](../done/neural-vision/assets/playtest/comparison.json). Differences in resolved content or outcomes require investigation, never tuning to force the counts.

Verify the shared emitter contract by moving/rotating an authored fixture and checking both rendered light position/parameters and simulation source coordinates. Verify both resolved exports include all matching sources and vision while preserving the other settings. Use meaningful consumer tests rather than duplicating constants.

Run native/client/web/renderer affected tests, type checking, WASM/web build. Serialize the actual resolved level modules for full-horizon native regression and actual worker runs; do not hand-build a parallel level for acceptance. Exercise both campaign rooms with16flies through complete outcomes, scoring, pause/seek/rewind, and return to editable setup with placements retained. Existing neural map and source-byte invariants remain green. Record any pacing errors or underruns and investigate them before accepting.

Use production screenshots for setup, active/selected fly, and replay. Compare with the accepted current house/panel captures using compare-screenshots; no artwork change is intended, so preservation is the target. Run fresh screenshot-critique as the final visual check. Keep the compact roster and graphs directly below the brain. Human feedback is nonblocking; preserve visible limitations instead of claiming they were fixed.

## Synthesis and bounds

Independent fewest-slices and seam-quality drafts agree on an authoring-time emitter owner and on separating ordinary campaign integration from puzzle efficacy. A third risk-first draft was canceled after the user explicitly narrowed the task to regression-safe wrap-up. This is now one contract, so no further multi-slice planning or experiment search is necessary. No requirement for an easier game, attraction or a successful new light-only puzzle remains.

Internal helper naming and test organization are delegated. The emitter settings, preserved gameplay settings and no-new-feature scope above are fixed. Review code shape, diff, docs and decisions, then close this spec and the updated user goal.
