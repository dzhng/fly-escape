# Closeout evidence claim audit

Read-only audit of the archived README overview, Evidence and rejected approaches, and Visual provenance against co-located artifacts and current code. No implementation changes or new simulation/playtest runs. The separate unfinished `specs/neural-vision/assets/playtest/` work is outside this audit and supplies none of its conclusions.

| Claim | Result | Evidence and limits |
| --- | --- | --- |
| Directional brightness propagates through the retained circuit; attraction, avoidance and puzzle utility remain unestablished. | Pass | Frozen neural analysis passes; motor pilot selections are null; all six production-body direction/mode summaries classify as `neither`. Correct scientific scope. |
| Seven prespecified contrasts, 847 simultaneous comparisons. | Pass | `neural-confirmation/freeze.json` fixes four opposing basis pairs, left/right lamps at rate 1, and rate 2 versus 0.5 on each side. There are 121 endpoint cells; 7 × 121 = 847. Analyzer computes two-sided Bonferroni 95% paired t intervals, df 29, critical t 4.695873299598626. Each contrast requires at least one significant cell, not every cell significant. Counts of significant cells are 14, 19, 21, 18, 41, 1, 1. |
| All 26 exact/graded controls pass. | Pass | `analysis.json` has exactly 26 passing gates. This includes the trivial `dark-inputs=dark-inputs` trajectory comparison, so 26 is an accurate gate count, not 26 independent experiments. Optical wall/furniture brightness equals dark; corresponding full-trajectory hashes agree for all 30 seeds. Opening matches the visible right lamp. Both graded-current checks pass. |
| Input silencing reproduces dark-inputs trajectories. | Pass | All 15 listed silenced-condition comparisons pass for seeds 100–129. Frozen probe hashes every measured voltage, spike and refractory state. This removes light-dependent activity while leaving spontaneous dynamics. |
| Whole frozen path-witness endpoint excludes injected cells and motor readouts; route is not uniquely identified. | Pass | 121 frozen endpoints have no input intersection. Probe constructs the union from audited path witnesses and removes inputs and motor readouts. Analyzer uses the entire vector for every contrast. Neither endpoint choice nor this result identifies a unique biological relay route. |
| Zero-outgoing sham validates machinery, not connected-cell specificity. | Pass | Frozen probe selects matched-count zero-outgoing cells outside inputs, endpoints and motor groups. Analyzer compares downstream voltage and spike vectors for left and right lamps. README correctly limits the inference. |
| Freeze, sources, individual seed evidence and independent analyzer are preserved. | Pass | SHA-256 of freeze, raw report and archived analyzer matches analysis identities. Tested map, tested manifest and probe snapshots match freeze identities. Current sensory, graph and LIF files also match frozen hashes. Git records freeze in 853efb6 before retained report in 721b691; run marker binds the same freeze hash. Source records cannot prove the absence of every possible unrecorded run, but support the stated recorded sequence. |
| Hard-sector pilot, DNp03 diagnostic and overlap pilot “all failed their motor gates.” | Wording defect | Both motor pilots have null selections and no candidate interval excluding zero. The DNp03 diagnostic instead tests paired **voltage** of a possible alternate readout and has `eligibleReadoutCandidate: null`; it is not itself a motor-output gate. Prefer “The motor pilots failed their gates, and the DNp03 diagnostic did not support a replacement readout.” No successful motor claim results from this defect. |
| No decoder change; failed motor work remains failed. | Pass | Current LIF source matches the confirmation freeze; diagnostic explicitly records no runtime modification or held-out confirmation. Archive retains null selections and does not relabel them successful. |
| Walking/flying panels resolve neither approach nor avoidance, despite individual trajectory differences. | Pass | `closed-loop/summary.json`: every distance-reduction and heading-alignment interval includes zero for left/right/forward under both starting modes. Each panel reports 90 changed lamp trajectories, 120 attempts, zero boundary ticks. These are descriptive unadjusted intervals, separate from the preregistered neural acceptance. |
| Final metadata performance: three paired 16-fly active workloads, median 0.78%, below 10%. | Pass | Recomputed from all six `performance/published` raw `wallSeconds`: paired overheads +0.7794457539%, −1.0599788514%, +1.7318376150%; median +0.7794457539%. All runs have 1600 active neural steps over 100 ticks. Off cues are empty; on cues contain vision gain 3. This is bounded native wall-time overhead, not sustained browser pacing or a statistically estimated population overhead. |
| Integration record distinguishes test/build/browser claims. | Pass with evidence limit | `integration.json` explicitly separates prepublication native suite, 29 connectome tests, client/web 38 tests with 360 expectations, typecheck, WASM/web builds, bounded browser run and independent review. These are recorded validation attestations, not all independently retained raw command transcripts. This audit does not claim to have rerun those suites. |
| Browser uses actual vision-enabled worker and exact native-record decoding. | Pass | Harness constructs `AttemptClient`, injects vision-only gain 3 for that worker, starts 16 flies, and waits for 30 ticks. Archived report records gain 3, first frame 16 flies and computedTick 30. Harness deep-compares decoded native fixture frames in seek order 1,3,2,4,1. |
| Campaign pause/seek/rewind and zero underruns. | Pass, bounded | Separate production-page segment waits for tick ≥30, pauses and seeks 10→20→10, comparing selected-fly text. Report records playing tick 30 and paused sample tick 10, both zero underruns, with no page errors. This does not measure indefinite playback or vision-enabled campaign behavior. |
| Campaign still has vision disabled. | Pass | Current `apps/web/src/levels/open-window.ts` tuning enables only excitatory/inhibitory odor (gain 2 each), with taste gain 1. Harness gain override is confined to its separate worker; production-page navigation does not enable campaign vision. |
| Selected-panel reference, prior directional frame and current captures are preserved. | Pass | All local links in the archived README resolve, including selected-layout reference, directional full-frame capture, three current captures, analyzer/harness and choices. Visual-review artifact explicitly distinguishes the earlier element capture from full-frame pixel comparison. |
| Independent visual review found no new overlap/clipping while retaining readability concerns. | Pass as attributed review | Co-located `browser/visual-review.md` records fully contained explanation and brain, no new structural overlap, plus clipped-at-viewport content, dense traces, scrolling discoverability and naming concerns. README accurately attributes that judgment and does not claim those existing problems are fixed. This audit checked provenance and wording, not a fresh screenshot acceptance. |
| About remains unchanged. | Pass at publication scope | Publication commit d47f260 does not change About; visual work concerns selected vision details and explanation. This is a historical change-scope statement, not a claim about separate in-progress work. |

The primary quantitative claims are supported. One terminology correction is recommended for the DNp03 diagnostic. No new behavioral acceptance or attraction claim is warranted.

Reproduction limit: system `python3` lacks NumPy, so this audit inspected analyzer logic and exact archived hashes rather than rerunning its SciPy calculations. Performance arithmetic was independently recomputed with standard Python. Original evidence was not overwritten.

## Resolution

Rechecked the sole corrected claim in the archived README: the hard-sector pilot, DNp03 diagnostic and overlapping-input motor pilot “did not establish a supported motor response or replacement readout.” This accurately covers both null motor selections and the null DNp03 readout selection without labeling the diagnostic a motor-output gate. The wording defect above is resolved; no open defect remains from this audit. No simulation or validation suite was rerun for this text correction.
