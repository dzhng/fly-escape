# Archived runtime and rationale claim audit

Read-only inspection at production commit `d47f260`; only this audit was written. Scope: README “Why this mapping”, “Boundaries that matter”, and every choices-ledger item. Campaign vision remains intentionally disabled at this checkpoint. No new gameplay acceptance is implied.

| Claim | Verdict and checked authority |
| --- | --- |
| MaleCNS source identity and optic-lobe columns, without measured world-angle registration | PASS. Live [official downloads](https://male-cns.janelia.org/download/) identifies the manifest's annotation source; [Explorer documentation](https://reiserlab.github.io/celltype-explorer-drosophila-male-cns/help.html#population-spatial-coverage) describes column coverage. Downloaded annotation bytes independently match manifest SHA-256 `2177e246…9a3b2`. `vision_map.py::preferred_angle` explicitly supplies the modeled conversion. No FlyWire identity import appears in the preparation/mapping pipeline. |
| Retinal R1–R8 absent; graph retains seed-touching edges | PASS. Independently joined the hash-verified source annotation table to current manifest body IDs: no selected `R1-R6`, `R7_unclear`, or `R8_unclear` cells. `extract.py` filters seed-touching edges before constructing the selected CSR. Manifest extraction provenance agrees. |
| Old AOTU populations lack columns; Tm2/Tm20 have annotated downstream witnesses | PASS. Frozen audit records 122 left/128 right AOTU cells with zero finite column pairs. Independently checked every accepted Tm2/Tm20 relay/readout witness edge against current graph: all valid, 366/810 inputs respectively. Reachability is correctly separated from measured activity. |
| Production uses smaller Tm2, selected by size for separate confirmation after failed motor experiments | PASS. Production `visionInput` exactly equals corrected `input-overlap/Tm2.json`. Preregistration in commit `62f064d` explicitly selects smaller audited Tm2 and untouched seeds after motor failures. Both pilot summaries retain no winner. Freeze and probe separate neural and motor endpoints. |
| Hex registration, cosine overlap, positive compression are modeling assumptions | PASS. `vision_map.py` owns mirrored ±π preferred angles, cosine support, column normalization and global row bound; `sensory.rs` applies brightness/(brightness+0.5), positive gain and final clamp. No physiological calibration is claimed. |
| Offline map ownership, single manifest runtime map, one-time validation | PASS. `prepare-graph.py` calls `vision_map.audit` and embeds the selected object; `Graph::from_bytes` checks it before use. `cue_currents` reads only `manifest.vision_input`. |
| Source/graph identity, non-motor cells, bounded nonnegative weights, equal directional dose | PASS. `graph.rs:215` validates annotation hash, graph identity, existing unique indices, motor exclusions, finite weights, row sums ≤1+1e-12 and equal nonzero column totals with relative tolerance 1e-10. The total-dose claim is within a map, not equivalence across families. |
| Extraction provenance and unchanged graph bytes survive publication | PASS. `prepare-graph.py` preserves extraction exporter hash/revision in metadata-only mode. Published manifest contains both. Current binary SHA-256 matches manifest `6218f745…cd454` and independently matches pre-confirmation commit `853efb6` graph bytes. |
| Visual adapter consumes only brightness; no blocked telemetry, exit bearing, camera or odor current | PASS for the visual branch. `sensory.rs:51` reads only the eight brightness values. Separate odor cue branches remain independent. |
| Brain mediates movement; existing dynamics/body decoding unchanged | PASS. `attempt.rs:363–384` samples, injects external current, calls Brain, then passes neural output to Body. Current LIF and sensory hashes match freeze. No LIF/body source or authored level differences against `853efb6`; no visual steering branch was found. |
| Enabled visual cue requires map; nonvisual synthetic graphs remain usable | PASS. Attempt construction probes each cue; nonzero visual gain errors without map, zero gain returns empty. Graph deserialization keeps vision input optional. Existing synthetic tests exercise map absence. |
| Recorded-sample contract survives | PASS. Attempt stores the exact sample used before the neural update; existing record/archive owners remain authoritative. Linked directional-vision rationale identifies the contract. Its old bilateral adapter description is a historical checkpoint, not the current mapping. |
| Display publication does not change authored body settings, timers, population or stars; campaign vision disabled; sconces decorative | PASS. Campaign level files remain unchanged across publication and contain only excitatory/inhibitory odor cues. Sconces are room-detail decorations; authored source content does not define them as light sources. Gameplay integration is accurately left unresolved. |
| Choices: modeled column registration; separate neural confirmation; positive graded bounded current | PASS, as above. These are correctly identified as chosen conventions rather than biological measurements or motor success. |
| Choices: manifest publication and attempt identity | PASS. `Attempt::describe` computes canonical hash of the whole manifest, including visual mapping, into `graph_manifest_hash`. |
| Choices: require each input's retained relay and movement route, exclude movement targets | PASS. Exporter checks both path hop counts ≥1 and motor exclusion for every accepted cell; independent CSR witness verification passed. |
| Choices: allow only numerical rounding at current ceiling | PASS. Loader tolerance 1e-12 and adapter clamp implement the stated bound. |
| Choices: freeze entire endpoint population, correct every comparison, preserve failures | PASS. Frozen endpoint indices/body IDs predate execution; analyzer computes 7 × all relay cells, uses Bonferroni paired intervals, and iterates every endpoint. Probe uses exclusive creation for confirmation freeze/run marker and keeps prior motor failures. |
| Choices: matched disconnected sham is a narrow machinery control | PASS. Independently checked all frozen sham cells have zero outgoing edges in current CSR and count equals 366 inputs; probe excludes inputs, relays and motor readouts. Narrow interpretation is appropriate. |
| Choices: extend existing active-work benchmark, retain default, reject inactive work | PASS. `examples/attempt_probe.rs` accepts optional level/tuning content, otherwise retains swarm default. Every measured tick requires the requested neural-step count and every fly's neural output. |

No unsupported or contradicted claim found in the assigned main-document/choices scope. No behavioral test or browser acceptance was rerun.

One low-priority historical-pointer caveat: `assets/input-map/README.md` calls today's `vision_map.py` the reproducible owner of its frozen hard-sector maps. Today's exporter produces overlapping maps. The newer overlap README correctly identifies hard-sector maps as historical evidence, but the old README's reproducibility sentence would benefit from a historical commit pointer. This does not contradict the assigned top-level rationale or production map identity.

Resolution: the historical-pointer caveat is closed. The hard-sector README now names commit `f669fdd` and distinguishes its exporter from today's overlapping projection. Independently inspected that commit's sector implementation and verified its exporter SHA-256 exactly equals `input-map/audit.json`'s frozen `exporterSha256` (`b941d61a5f9a124d4e06220f561b768c0c476f3ef622c3823d737ea0c66f619f`). No outstanding issues remain in this audit.
