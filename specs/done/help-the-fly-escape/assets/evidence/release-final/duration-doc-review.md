**Pass.** `specs/done/help-the-fly-escape/GAMEPLAY.md:35` now states both halves of the claim explicitly — "The user allowed sixty-minute diagnostic experiments to distinguish slow progress from trapping. Those require a scratch build; shipped attempt validation retains its ten-minute cap." — and closes with the matching instruction not to extend the horizon beyond ten minutes. That is consistent with the code: `crates/sim/src/attempt.rs:52` defines `GAME_TICK_SECONDS: f64 = 0.1` and `crates/sim/src/body.rs:394` rejects any level with `duration_ticks == 0 || duration_ticks > 6000`, so the hard shipped ceiling is 6000 × 0.1 s = 600 s = exactly ten minutes, and a sixty-minute run (36000 ticks) would be refused by the stock validator, which is precisely why it needs a scratch build. The Fast-playback sentence at line 37 ("a ten-minute attempt uses 10×", i.e. 600 s ÷ 60 s) corroborates the same ceiling rather than contradicting it. No defect found.
