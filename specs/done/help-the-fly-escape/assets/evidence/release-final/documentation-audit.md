## Findings

**1. `specs/done/help-the-fly-escape/choices.md:100` — wrong constant (contradicted by code).**
Claims flies are "enlarged at wide zoom toward a 24 CSS-pixel target". The shipped constant is 48: `packages/game-renderer/src/camera.ts:14` (`READABLE_PIXELS = 48`), consumed at `camera.ts:182` with the 0.8 exponent and the room-fraction ceiling. The rest of that sentence checks out (ceiling `MAX_ENLARGED_ROOM_FRACTION = 0.03`, exponent 0.8). 48 is also the value consistent with `CONTRACTS.md:81` (40–60 px) and the close-follow divisor at `camera.ts:74`. Correction: "toward a 48 CSS-pixel target".

**2. `specs/done/help-the-fly-escape/GAMEPLAY.md:35` — allowance the shipped code cannot honor.**
"Diagnostic runs may extend to sixty simulated minutes to distinguish slow progress from trapping." Every attempt is validated at ≤6000 ticks (ten minutes) in `crates/sim/src/body.rs:394` and `packages/sim-client/src/record.ts:89`; `CONTRACTS.md:65` states the same 6,000-tick maximum. The 60-minute diagnostics that were run used a scratch body (`assets/evidence/33/clustered-bananas/README.md:14`: "Its scratch body permits longer diagnostic horizons than production"). Correction: mark it as a deferred allowance, e.g. "sixty-minute diagnostic horizons require a scratch build; shipped validation caps every attempt at 6,000 ticks".

**3. `specs/done/help-the-fly-escape/README.md:35` — provenance claim partly unsupported.**
"The approved mock review records the user's close framing, world/card selection and readable trails." `assets/ui/mock-review.md:13` records the user feedback as close default camera, world/card follow selection with adjustable zoom, 3D Blender assets and dead-end rooms. Trails appear only as a host design control (`mock-review.md:5`), not as recorded user feedback. Correction: drop "and readable trails" or attribute it to the design controls rather than the user.

**4. `specs/done/help-the-fly-escape/README.md:13` — pointer resolves to the wrong file.**
"residual cutaway-wall bands and other cosmetic issues are recorded in [presentation evidence](assets/evidence/31/README.md)". `31/README.md` does not mention the bands; they are in `assets/evidence/31/foreground-walls/README.md:7` ("translucent foreground planes form pale overlapping bands"). Correction: point the band clause at `assets/evidence/31/foreground-walls/README.md` (`release-final/visual-verdict.md:6` covers only "macro wall/ground artifacts and retained white trails").

**5. Minor line-range drift (claims correct, ranges slightly off).**
`choices.md:77` cites `placement.rs:96-110` for crumbs/lamp/shade; the block is 98–111 and `Shade` sits at line 111, just outside. `choices.md:34` cites `sensory.rs:314-395`; the file ends at 394.

## Verified

**Pointers and links.** Every markdown link in the five documents resolves, including all evidence directories (`release-final`, `33/graded-browser`, `33/scent-strength`, `33/global-scent`, `33/clustered-bananas`, `32/food-strategy` + `biology.md`, `31/`, `31/foreground-walls`, `24/sims-walls`, `spikes/`, `superseded/`, `assets/planning/synthesis.md`, `visualizations/ui-mock.fragment.html`) and every root-README target. All cited code paths exist; line numbers land on the claimed code except as noted above.

**Graded odor (N3, shipped README "Findings", CONTRACTS:53).** `sensory.rs:19` `ODOR_HALF_CONCENTRATION = 0.55`; `sensory.rs:63-79` graded `c/(c+0.55)` on the winning side only, vision fixed at 1.0, 0.05 cue floor and 0.01% contrast floor, gain clamp; motor readouts excluded (`sensory.rs:98,107`). Property tests at `sensory.rs:316-392`; `crates/sim/tests/attempt.rs:430-441` composes adapter output rather than `count × gain`.

**Campaign content.** Both levels: `exitSuction {reach 0.9, speed 0.5, roomSpeed 0.06}` at line 33; thresholds 1/8/15; `zappers: []`; 20 flies (`setup.tsx:349`) with `flyingCount: 10`; inventory holds one fan and no crumbs/lamp/shade. Level 1: 5 rooms, 6000 ticks, 0.12/0.24, fan heading π; level 2: 6 rooms, 3000 ticks, 0.24/0.48, fan heading 0; web at `open-window.ts:87`, zapper at `turn-the-corner.ts:105`. `levels/routes.test.ts` reads the same literals and pins ≥3 doorway crossings, full connectivity, >9 m spawn–exit distance and a one-doorway wrong-turn room.

**Suction, outcomes, timing.** `body.rs:341-372` gates the pull on exit-room membership plus line of sight, takes the larger of room-wide and near-door speeds, targets a point outside the opening; escape only via swept crossing (`body.rs:808`), zapper regions swept at `body.rs:814` and validated at `body.rs:387-407`; outcomes exactly the five at `body.rs:56-61`; `LifeModel::Timed` at `body.rs:112-120`; `GAME_TICK_SECONDS = 0.1` at `attempt.rs:52`; `zappers` field at `attempt.rs:69`.

**Retired-content concern (U1).** Confirmed as described: crumbs/lamp/shade carry sensory effects, appear in `tool_catalog()` (`placement.rs:151-170`), are wired in `assets/tools/registry.ts:4,7,8`, and `placement-models.ts:111-112` silently skips unresolved kinds while throwing on a missing catalog entry.

**Playback, transport, archive.** Fast = `max(1, duration·0.1/60)` (`playback.ts:14,42-45`) → 10× and 5×; campaign defaults to fast (`playback.tsx:219`); `timedOut` renders as "dead" (`playback.tsx:547`, `science-panel.tsx:248,292`); reserve UI exists only in the lifecycle lab. Schema 4, 10-tick chunks, 128 MiB cap, 8 events/fly/tick (`record.rs:21-26`), ≤16 groups (`record.ts:90-91`); generation + attempt-ID gating (`attempt-client.ts:66-70`) and worker retirement (`attempt-client.ts:54-55,94-106`). `parry3d-f64 =0.30.2` + `enhanced-determinism`, `float_roundtrip`, wasm-only `libm` (`Cargo.toml:8,12,16`); `three@0.185.1` patch present.

**Steering and presentation.** `lif.rs:33-35,351-380`: ×4 right-minus-left spike fraction for the olfactory turn, voltage readouts elsewhere and for the fallback; the same term adds into `flight_turn` (`lif.rs:397`). Eight-second departure (`departure-tail.ts:21`), 5× wing rate (`fly-motion.ts:9`, `fly-departure.ts:15`), quarter-turn-only furniture (`environment/mod.rs:73,201-206`), palette → `house-materials.ts:2`, in-scene orientation-only reset (`camera.ts:140`, `camera-input.ts:5-15`), no Overview button, "Release the flies"/"Put objects away" present, About as a root query (`main.tsx:250`) with `/lab/*` routes including `/lab/brain` as the fallback.

**Evidence numbers.** `release-final/README.md` supports 2 escapes house 1 / 6 house 2 against threshold 1 (N1), the 85.7 s and 38.8 s waits plus the ~115 s standalone run (shipped README:13), and the Firefox/WebKit smoke scope. `33/graded-browser/README.md` supports "5 with a banana versus 2 without" (N3). `30/refinement-contact/README.md:11` supports the 106 declined-refinement body-ticks. `33/global-scent` and `33/clustered-bananas` support the "not adopted / did not outperform" framing; shipped emission rates in `placement.rs:60-111` match choices item 4 exactly (1.0 apple/banana/crumbs, 1.5 dishes, 0.35 shoes, 0.2 laundry, 1.0 vinegar, radius 0.75; cat and web emit nothing).

**Visual provenance.** `assets/ui/` holds `mock-review.md`, `review-default.png`, `review-close-crop.png` and `rejected-focus-indicator.png`; `24/sims-walls/` holds `before.png`/`after.png`; `release-final/` holds the campaign, platform and About captures plus `checks.json`, telemetry, logs and `visual-verdict.md`.

Two things I could not check under the read-only, no-build constraint: the recorded test counts (165 sim / 31 client) were taken as recorded in the evidence, not re-run; and `release-final/README.md:23`'s claim that the only simulation-source change since the captured telemetry is the evidence-link comment was checked only against the comment text at `sensory.rs:16-18` (Bash, and therefore `git diff`, was denied in this session).
