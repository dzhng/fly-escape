diff --git a/crates/sim/examples/campaign_probe.rs b/crates/sim/examples/campaign_probe.rs
index f9541fc..b3a37a6 100644
--- a/crates/sim/examples/campaign_probe.rs
+++ b/crates/sim/examples/campaign_probe.rs
@@ -214,12 +214,12 @@ fn main() -> Result<(), Box<dyn std::error::Error>> {
     drop(bytes);
     let graph_load_seconds = start.elapsed().as_secs_f64();
     let mut pairs = vec![];
-    for &seed in seeds.iter().take(count) {
+    for &seed in seeds.iter().skip(1).take(count) {
         let mut conditions = vec![];
-        let empty = vec![];
+        let empty: Vec<_> = content.reference.iter().filter(|p| p.kind != sim::placement::ToolKind::Fan).cloned().collect();
         let mut arms = vec![("reference", &content.reference), ("poor", &content.poor)];
         if args.len() == 7 {
-            arms.push(("empty", &empty));
+            arms.push(("no-fans", &empty));
         }
         for (label, placements) in arms {
             let row = run(&graph, &content, seed, label, placements)?;
