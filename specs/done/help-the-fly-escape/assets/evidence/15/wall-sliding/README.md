# Timed wall sliding

The body reaches a wall at its actual impact time, retains tangential velocity, and sweeps the remaining travel against other walls. Packed replay contains the contact bend. Straight visibility and field sweeps retain their existing semantics; this does not steer toward doors or change neural outputs.

The oblique-wind body regression fails with the old motion implementation and passes after integration. The merged body, environment, house-geometry and record suites pass (47 tests). WASM rebuild, TypeScript and web production build pass.

Actual Chrome/WASM seed 1000 first-level completion: 18 escaped, two starved, three stars, result UI, retry and earned second-level unlock; no browser errors. The current furnished second-level native diagnostic with six fans produces three escapes against zero empty. This is a diagnostic, not campaign acceptance. The first fan moved to clear the newly placed chair, so this pair is not an isolated numerical comparison with older room data.

Review: the existing slab collision primitive supplies contact axes to the body's timed motion owner. No second path sampler, steering controller, schema or tolerance adjustment was added. The explicit oblique and corner assertions verify behavior rather than mirroring implementation. Remaining second-level route and food dependence are tracked by the campaign slices.
