# Finish marker and inventory verification

The Finish plaque lies on the floor inside the exit, follows its orientation and uses normal depth testing. It belongs to the exit's existing resource lifetime and affects no simulation fields or collisions. It renders after the additive spill so the objective text remains legible.

Both campaign inventories resolve through the native setup path with triple non-fan quantities and one fan. The final-item placement regression pins its own stock at the worker boundary, preserving its exhausted-inventory coverage independently of campaign tuning. That browser regression passes, along with type checking, all renderer/web tests and the production web build. Independent code review found no actionable regressions.

Full overview and close captures reproduce both opposite-facing exits. Fresh visual review identified the second exit frame obscuring letters, so the plaque moved inward. The regenerated review confirms every letter readable in both views. At the second exit's close angle the post still naturally occludes a blank corner of the plaque; the word stays clear. Enlarged crops preserve the depth and readability checks.
