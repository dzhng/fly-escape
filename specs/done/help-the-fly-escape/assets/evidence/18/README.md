# Seeded starting swarm checkpoint

Rust owns the authored `SpawnDef` and every resolved initial body. Campaign clusters sample up to 128 candidates per fly, clear geometry, hazards and earlier bodies, use full-circle headings, and assign exactly the authored flying count through a seeded ordering. SHA-256 pose and mode domains never consume the neural RNG. This changes initialization only; existing sensory, neural and body equations own subsequent motion.

The first candidate uses ten walking and ten flying starts. That balanced ratio and the cluster dimensions are delegated authoring choices, not user-specified values. Whole-area tool exclusion expands the authored rectangle by body/tool clearance before a seed exists. The setup view labels this reserved area and shows no speculative flies. Controlled scientific fixtures retain explicit fixed starts, including intentional hazard contact or overlapping bodies used in their tests.

`AttemptInfo.initialBodies` snapshots actual bodies at construction. The archive, first rendered pose, camera and science cards consume it at tick zero; rewind restores the same modes and positions. The spawn-area annotation owns its line, sprite and texture, disposes replacements, hides unstarted flies from selection, and is removed when poses appear.

Fourteen focused Attempt tests pass, including same-seed reproduction, changed-seed variation, mode counts, representative heading quadrants, hazard/solid clearance, spacing, bounded exhaustion and real placement exclusion. A neutral-input actual Brain comparison proves initialization does not consume neural noise. Fixing all headings at zero makes the heading-distribution test fail; restored source passes. The existing house/greybox/probe tests pass, as do 47 client/renderer tests and two WASM-session tests. Type checking, a fresh WASM build and the production web build pass.

`transport/report.json` records three three-tick runs through the actual Graph and WASM Worker: seeds 42, 42 and 43. Tick-zero bodies agree with the first frame's input poses, the repeated seed reproduces exactly, the changed seed differs, and archive mode reconstruction returns to the initial mixture. These are functional transport checks, not campaign calibration or performance measurements.

`final/` records actual setup → Run → pause at zero → overview → early playback → rewind to zero. Each state has a full screenshot and world/detail crops. `draft-unlabeled/` preserves the rejected ambiguous outline and incorrect area crop; `draft-occluded/` preserves the rejected annotation occluded by a wall. Final evidence uses consistent page framing and a readable annotation; the science panel may scroll the selected card within its own pane.

Fresh reviewer capacity was unavailable. Root reviewed the initial five fulls and five detail crops using the screenshot-critique skill's adversarial fallback, with prior source-review priming disclosed. It identified the unlabeled outline as a high-confidence defect and accepted varied heading/height and rewind evidence as an initialization foundation, with fly scale and furnished realism still open. Root then inspected all five refreshed fulls and the area detail. Its adversarial verdict: the label addresses seam ambiguity; distinct headings, airborne shadows and walking/flying cards support varied starts despite overlap; release/rewind world agreement supports restoration. It accepted the initialization visual foundation, while selected-fly overlap remains the later readability slice. This is a disclosed primed adversarial fallback, not an unprimed review claim.

The new starts invalidate earlier content difficulty acceptance. Open Window is explicitly unfrozen. This checkpoint does not accept photorealistic furniture, final scale, lighting, the two final levels, or their difficulty. The old heldout run remains evidence only for its frozen earlier scene and starting states. No new calibration batch ran here. Preview is owned by root and was not opened by this agent.

Root integration checks also pass: fourteen native Attempt tests, client/renderer tests, typecheck, rebuilt WASM/web, actual Worker start transport and the built setup/release/rewind workflow. The three-shot human checkpoint was open from 03:14:29 to 03:19:35 UTC on 7 September; no new direction arrived. Root proceeded on the recorded evidence and closed Preview. This is acceptance of initialization only; scale and furnished readability remain open.
