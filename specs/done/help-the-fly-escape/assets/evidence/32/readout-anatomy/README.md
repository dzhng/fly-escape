# Anatomical readout correction

The two source-annotated intrinsic cells are excluded from the olfactory descending readout. The lateral-horn candidate remains in its sensory group; the existing input/readout exclusion guard is unchanged. This corrects classification, not a measured biological turning function.

Full-source export and the independent signed-edge oracle pass: all selected bodies and signed connections are unchanged. Only OLFACTORY_DN_RIGHT pathway membership changes. The metadata and derived group links were regenerated and copied into the static browser build. The browser neural-lab check passes reset reproducibility, sensory perturbation, pause, fault/retry and malformed-manifest rejection, with no page errors. These checks establish integration, not chemical encoding or campaign balance.

All eleven graph tests pass. Regression tests reject intrinsic, wrong-side and missing-annotation candidates through metadata export. The implementer confirmed those rejection tests fail when validation is removed; the old registry fails the source-annotation check. Root repeated the suite and full-source validation. Independent Codex review found no actionable regressions. Root moved the source-identified annotation fixture under tests so spec archival cannot break the suite.

No current gains, dynamics, matrix extraction or physical parameters were changed. The model still uses hypothetical odor-to-lateral-horn stimulation. The prior negative DM1 sensory-input experiment remains relevant, and must not be erased by this correction. Next work is a bounded, biologically grounded encoding/circuit experiment, not retuning readout weights for a desired escape score.
