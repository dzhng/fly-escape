# Baked apple skin

A bounded material improvement replaces the repeated radial orange stripes with irregular red/yellow blush and scattered pale pores. The single local image is authored deterministically in Blender's bundled NumPy, on spherical coordinates with wrapped longitude. It exports as ordinary glTF base color in sRGB, with no runtime procedure, service, new dependency or extra material/draw. Existing roughness and clearcoat remain unchanged. The shared helper still owns the banana's identical color formula; its shipped GLB, blend, thumbnail and contact were not changed.

Close realism remains open. The pale pores are soft at fly-close framing, broad highlights still look plastic, and one broad yellow/orange patch can read as a band. Dark fly legs have slightly lower contrast against deep red but remain traceable. The independent reviewer prefers the candidate over the original for apple skin identity; the regular stripe pattern is gone in inspected views. Geometry/silhouette, contact, fly pose and accepted selection marker remain unchanged.

The exact canonical surface comparison passes, including vertex/triangle order: 762 vertices and 1,520 triangles. UV seams duplicate only render vertices. The old diagnostic workbench incorrectly compared raw render vertices; separate commit 07e97ef routes it through the existing canonical contact owner. Its browser gate accepts the native textured apple and rejects a 100 micrometre vertex displacement. The failed pre-fix load screenshot is preserved. No mesh repair, tolerance or contact fixture change was needed.

Evidence uses the production renderer in the existing native-contact workbench, at fixed core-provided body poses. It is a controlled material/attachment comparison, not a neural food-seeking or apple-feeding result. Existing contact harness assertions and reverse-seek pixel restoration pass. Full views and contact crops accompany measured pixel changes. The overview is too distant for material judgment; whole uses the same supported follow target and a +1500 wheel delta in both arms. Grass in this metre-scale diagnostic room is incidental and outside this material pass.

The shared model/lighting thumbnail helper regenerated fruit.png. Actual first-house tray captures compare the old and new image at 48 px. Baseline GLB and thumbnail responses were substituted only for browser fetch/image requests; production code and camera stayed identical. Only one material candidate was authored; no broad tuning sweep or rejected texture alternatives were accumulated.

Validation: 49 renderer tests, both fruit contact export tests, TypeScript and the browser contact validation/pose gates pass. `validation.json` records exported counts, glTF material and memory estimate. One 2048×1024 image costs approximately 10.67 MiB as RGBA with mipmaps, shared by native model instances. Direct code review checked UV topology, image ownership, unchanged banana math and exact contact. Independent Codex CLI review remains unavailable because its configured model requires a newer CLI; no dependency/tool upgrade was attempted.

## Choices audit

- **Unique image instead of repeated detail — sound, medium confidence.** When inspecting an apple close up, regularly repeated stripes or spot groups look painted. A single image gives this apple one irregular arrangement of blush and pores. A repeating tile would offer sharper detail at the same memory cost but reintroduce patterned clusters. Texture layout was delegated; changing the image later leaves physical contact untouched.
- **Retain smoothness/lighting — sound, high confidence.** The apple still has broad glossy highlights because the pass changes skin color only. Adjusting roughness or lighting could make it look less plastic, but would mix variables reserved by the slice. Those limits remain visible in the evidence.
- **Bounded improvement — sound, medium confidence.** Replacing the regular stripes improves the shipped view despite soft close pores. This does not close slice 22 or establish photorealism. The retained original images show what improved and what remains.

Final independent whole/tray review accepts the bounded skin improvement, with high-confidence remaining limits: the broad orange side stripe and bottom patch look painted, shine remains plastic-like, and the new tray icon is darker and less obviously apple-shaped in isolation. Red fruit plus the Apple label remain readable, without clipping or layout regression. No claim of final whole-apple realism is made.

Root integration passes both contact export tests, all renderer tests, typecheck and both production builds. The workbench's valid/mismatched asset gate and full contact/animation reverse-seek gate also pass. `integration.json` verifies all three merged supported-pose captures are pixel-identical to the independently reviewed candidate and differ from the original. The rebuilt static game's `merged-tray` captures verify its matching native apple image loads without page errors. Raw pose reports are gzip-compressed.

Whole/close/prior-close shots were opened together in Preview for a five-minute non-blocking review. No user response arrived; the independently accepted bounded improvement is retained, with final realism still open, and Preview was closed.
