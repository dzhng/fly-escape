# Graded odor input in the browser

Detected odor concentration now controls the winning side's current through the tested bounded curve. Normal emission applies consistently to objects; no banana-specific or global amplification ships. Vision remains binary. Neural dynamics, source memberships, readout exclusion, physical rules, timer and star thresholds are unchanged by this pass. The sensory adapter remains a model, not a measured receptor response.

Actual browser attempts use20 campaign flies, seed15789670027162723101, normal emissions, current gentle room pull and the ten-minute first level. These are functional comparisons, not statistical balance acceptance.

| Encoding | Added object | Escapes |
| --- | --- | --- |
| Binary | None | 3 |
| Binary | Banana | 3 |
| Graded | None | 2 |
| Graded | Banana | 5 |

The empty binary baseline is in [room-pull evidence](../../15/final-room-pull/README.md). The banana is placed through the actual UI in the final-room entrance. Graded paired browser runs were concurrent: the banana had zero underruns, the empty had one. A standalone repeat preserved the empty result2 with zero underruns. That repeat buffered115.3seconds and frame-p95 was34ms: startup and rendering targets are not met. The user subsequently authorized shipping with somewhat-working mechanics rather than extended playtesting/balancing; performance targets remain disclosed limitations, not claimed passes.

Verification:165 simulation tests passed with one pre-existing ignored test;31 client tests passed. WASM, typecheck and production builds passed. Independent code review found no actionable defects. Tests retain detector/side/motor-exclusion behavior, verify bounded dose response in both odor channels and fixed vision, and check actual channel summation plus ablation/replay in Attempt. A mutation to fixed amplitude fails the dose tests.

The curve changes effective drive as well as adding concentration dependence. A one-seed browser improvement supports playable experimentation, not a guaranteed food strategy or biological validation. Retained raw reports contain build/content identities and timing. No new art was introduced.
