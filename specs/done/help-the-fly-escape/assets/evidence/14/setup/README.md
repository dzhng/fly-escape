# Setup and attempt review

The fixture exercises editing and attempt ownership, not campaign efficacy. Its Rust content owner reads the same house geometry as asset review. Tool positions are proposed in the browser but accepted only by the simulation's whole-setup validator. Hover validation retains one pending request and the latest pointer intent; committing an edit prevents later pointer movement from replacing that edit.

One Worker survives setup, attempt and retry. Setup operations instantiate no graph or neural state. The attempt view consumes the same archive, playback cursor, selection and science panel as the playback lab. Completion awards only best stars; replay reads records and does not submit another result. Only editable setup, preferences and best stars persist, so reload has no unfinished attempt to resume.

The [browser contract test](../../../../../../../tests/browser/setup.mjs) exercises invalid placement followed by a valid edit, move, fan rotation, refund, freezing, cancellation, unfinished reload, fresh seeds, completion, replay, preference restore, explicit reset and denied storage. The initial run exposed an invalid-click lock; the test failed waiting for subsequent valid placement, then passed after rejected commits released the editor. Native placement tests, client tests, renderer tests, TypeScript and the production web build also pass. The existing real-WASM placement boundary test passes unchanged.

[Final captures](final) include full views and enlarged control/world crops, plus the real-attempt report. `result.png` now shows the ended cursor and authoritative result. [Rejected captures](rejected) retain the draft's low-contrast styling and an incorrectly labeled replay capture. Pixel telemetry compares the initial draft with the final setup at the same viewport; it proves the contrast pass reached the browser, not a campaign-art acceptance.

A fresh reviewer found the initial tool label wrapping awkward and the first detail crop incomplete; both were corrected. The original “result” capture was running replay and was replaced with an ended-state capture. Final independent visual review accepted the complete set: the remaining pointer-canvas focus outline was removed and the completed-result crop now contains the selected fly/ring. Nearby wall occlusion leaves the body/selection association clear. The reviewer inspected all prior images and the four changed images, confirming the other eleven were byte-identical. Best-star preservation also has a unit test falsified by replacing best-so-far with latest-result, then restored green. Preview opened at approximately 18:46 UTC for the nonblocking human checkpoint; no campaign decisions depend on it. Codex CLI review was attempted but could not run: the installed CLI's backend rejects the configured model as requiring an upgrade. No alternate model or installation was chosen.

Final decorative props, campaign inventory and difficulty remain later slices. Generic colored tool markers make placement and fan direction inspectable without claiming final art.

The delayed-reply browser regression holds actual WASM Worker replies at the browser boundary, commits a placement, then physically clicks a different palette tool or placed-tool selector before releasing the reply. Both committed placements must remain. It failed on the lost placement before the UI disabled intent-changing controls for committed edits; it passes with the fix. Hover-only validation still permits replacing the latest candidate.
