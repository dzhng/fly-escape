# Core preparation integration

The root now includes independent attractive/repellent transport, simultaneous sensory input, local fans and canonical placement resolution. The prior heading identity fix is included. Native gates pass: 10 attempt, 13 environment, 4 placement and the shared house topology fixture; Clippy passes. Client16 and renderer12 tests pass (including the prepared trail seams). Typecheck required installing the already-declared workspace links after the house package dependency merged, then passed.

The real WASM placement boundary passes; production browser fields preserve mirrored odor, opposite light/shade, local exit and wind behavior with no page errors. The production playback control suite passes pause, seek, 2×, replay, restart and sample-time alignment. Adjacent integrated-fields and integrated-playback folders preserve those artifacts. Existing single-cue trace parity and independent source reviews remain linked from the preparation evidence.

This does not make the entire placement/game slice complete: the editor is still isolated, and mixed-cue campaign performance and balance need their own measurements. The ten full baseline runs refer to the earlier build identity. No neuroscience description was replaced with gameplay advice during integration.
