# Game view without the technical banner

The normal attempt keeps outcomes and neural education visible while simulation identity stays in the console. The diagnostic lab retains its banner. Removing the banner also returns its reserved vertical space to the scene; narrow counters have room to wrap.

The complete desktop candidate set compares against the same frames in [the preceding ring capture](../../20/ring-contrast/after). Pixel differences are measured in `pixels.json`; scene framing changes because its viewport gains height, while the sidebar and playback controls remain unchanged. Reverse seek still restores identical canvas bytes. `narrow.json` confirms counters end above the canvas and no horizontal overflow at 390 CSS pixels. `production.png` and `console.json` additionally verify the actual game route emits attempt settings without displaying the technical banner; this route capture predates the final spacing correction.

Independent review accepts all eight final desktop states and the narrow view: counters wrap clearly, the selected fly and controls remain visible, and no new overlap appears. The initial removal left empty space, preserved in `rejected-empty-strip.png`; final layout reclaims it. A pre-existing blank gap below the narrow canvas and prominent white trails remain outside this pass. TypeScript checking passes.

Shape/diff review retains the existing player and console owner, with one game-specific layout class and no new state, dependency or diagnostic mechanism. The user delegated technical information to the console; no new product or architecture choice is introduced.
