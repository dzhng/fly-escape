# Full timed house attempts with firing-based steering

Eight native attempts with the exact `candidate.patch` and `plans.json`, two nonoverlapping seed processes,20flies and3000ticks. The existing second-house-route harness was reused verbatim. Current paced house, gain two, unchanged geometry/content. Every attempt completed, every fly has a terminal outcome, and level/tuning hashes match the corresponding baseline content. Raw reports retain source/build identities.

| Placement | Production110,111 | Candidate110,111 |
| --- | --- | --- |
| Empty | 4,3 | 2,3 |
| Corner fan | 1,5 | 3,5 |
| Fan plus rear vinegars | 4,6 | 4,4 |
| Study food | 0,3 | 0,5 |

Total escapes are unchanged; per-fly trajectories change. This proves complete timed execution, not improved campaign balance. The source/readout combination implements the polarity measured in the spatial experiment. Do not interpret a flat total across different strategies as evidence that the attraction mechanism is inert, or as proof that it makes a better puzzle.

The delegate suggested rejecting the candidate because total escapes did not improve, then testing a binding-only reversal and smoothing. Root did not adopt those suggestions: the user wants discoverable object effects and accepts unequal strengths; controlled spatial evidence, followed by actual browser play, is the relevant decision basis. No more parameter sweeps are justified by this table alone.

The delegate could not locate two original gain-two baselines and marked those comparisons weaker. Root had already compared all six corresponding gain-three per-fly aggregates against their original gain-two files exactly when banking `remaining-timed-pilots`. Those historical outputs are preserved there and in the pacing/ablation evidence. These remain small tuning pilots, not final holdout evidence or performance measurements.
