# Escaped fly cards

Escaped flies replace the roster preview with a green check in a soft circular badge. The fly number and status remain in place. The icon follows the viewed frame’s outcome; the preview canvas stays mounted so rewinding can restore the fly normally.

The real-WASM exit fixture checks no badges before escape, all sixteen badges after escape, and sixteen visible preview canvases with no badges after rewinding. Typechecking, web tests and the production build passed. Root inspection and a fresh review of all six screenshots/crops found legible, centered checks, consistent alignment, and no card clipping or overlap.
