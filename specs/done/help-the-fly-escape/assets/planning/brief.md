# Independent browser-game plan brief

Read-only planning; no implementation or edits. Inspect actual repo code and /Users/david/dev/game reference architecture. Produce a complete slice graph with seams, dependencies, playable/inspectable outputs, test gates, evidence, risks and delegated choices. Do not read other drafts. Keep draft under 1800 words.

User decisions: fully local browser game (static hosting/dev localhost okay; no backend), desktop/laptop first, similar structure to ~/dev/game. Rust/WASM heavy sim + TypeScript shell is recommended but planner must decide/justify. Fully 3D rooms/props/flies/camera; use available Blender MCP for assets. Current UI mock illustration is not production renderer. Five authored levels by agent, increasing difficulty and room counts, first level has five rooms. Dead-end rooms with one doorway intentional; not every room is a passage. No separate tutorial: first level teaches. Fixed tool inventory, no shop/currency. 20 independent flies each level; 100 supported later, not required MVP optimization. Fresh random seeds each attempt, replay preserves exact attempt. Sequential unlocks at 1 star; per-level thresholds. Finite replenishable energy reserve, food stops matter on larger maps, neural feeding required, only escape scores. Place/move/remove pre-run; freeze during attempt; retry retains setup as sensible displayed defaults. Buffered compute-ahead playback, may start before sim complete, smooth rendering/no repeated stuttering. Fixed angled dollhouse camera, close default; whole-house view minimum zoom; click fly model or its right-side card selects and locks close follow, camera autopans to selected fly while zoom remains user-adjustable; manual pan/Overview releases follow proposed. Rotation later. White fading trails, yellow selection circle ONLY (no thick black/blue HTML focus ring on pointer click; retain accessible keyboard focus). All 20 fly cards in scrollable right panel each with both grouped-pathway network and time traces, selected card scrolls fully into view. Data synchronized to playback. No individual-neuron full voltage histories. Plain language plus detailed neuroscience tooltips for curious friends who know no neuroscience. Cool whimsical house with warm orange exit; approved mock UI, close-up fly models need actual 3D. No compatibility or data migrations; replacement/deletion authorized, preserve reference evidence before removing.

Evidence: current Python src includes mature Origin sim but data only .gitkeep. graph_loader requires three short-named feathers in data; JS downloader fetches different significant-only files into data/raw. visual_motor selected target ~70k, seed-touching edges not induced graph. lif_sim sparse matvec + neuronwise noise, one LIF tick per game tick, game time 0.1sec/tick; original performance not browser proof. feeding_maze_sim EXIT ordinary Gaussian no LOS/room gate; timeout with any meal returns WON and swarm counts WON escaped. landing_feeding contact+hunger bypasses MN feeding; one meal exempts lifetime starvation. AOTU light/shadow injections added identically; wind advection exists elsewhere but feeding loop uses static Gaussians. Need controlled reproductions and semantic repair, not blind golden of bugs. Existing spec old Python 3-slice techdemo excludes browser/100: superseded by this request. Map specs/help-the-fly-escape/BROWSER_GAME_MAP.md. UI mock in /Users/david/.codex/visualizations/2026/09/06/01a0762f-9d54-7f42-ada1-774575422d38/fly-escape-mock.html. Build authorization not yet requested: deliver spec only.

Every visual slice needs screenshot-critique last; compare-screenshots if reference exists; one visual variable/crop per slice. Human review nonblocking (~5min feedback while useful work continues, evidence decision if silent). Missing art can use replaceable 3D placeholders; synthetic graph allowed only harnesses clearly labeled, never product proof. Name single owners; avoid generic infra/compat layers. Exact tuning is delegated within explicit success metrics. First useful checkpoint must show an actual graph-driven fly and telemetry before long asset/campaign work.
