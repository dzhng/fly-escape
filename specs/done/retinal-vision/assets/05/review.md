# Slice 05 preparatory review

Verdict: the provisional exporter and adapter oracle pass their offline gates. Final optical-profile binding, browser landmarks and downstream neural proof remain gates for later integration; this artifact is not published into graph metadata.

All 37 connectome tests pass using the project NumPy/pandas/SciPy environment. Focused contracts cover local triangles, full-source registration independent of graph selection, shared family coordinates, dose saturation, sparse support, identity rejection, negative/nonfinite or rank-collapsed coefficients, motor exclusion, and supported chromatic swaps with preserved actual current. The first geometry/export tests failed before implementation. The independent review reproduced a real rig identity failure: a changed height and quaternion could retain a claimed hash. The final validator checks source-canonical rig bytes, SHA and parsed content; its regression rejects changed transforms under unchanged identity. The reviewer’s default Python lacked NumPy; the complete suite passed separately in the project environment.

Two final real-data CLI exports are byte-identical; `reproducibility.json` records artifact hashes and unchanged graph/manifest hashes. Raw barycentric reconstruction has maximum error 2.435e-15 sample-lattice units across 1,176 entries. There are 366 Tm2 and 810 Tm20 eligible neurons; three Tm20 left-eye neurons lack columns and are rejected. Retained sample counts are Tm2 L/R 155/175 and Tm20 L/R 188/205 of 721. Full-source sample counts are 242/241 and 239/238 respectively. The atlas distinguishes these two kinds of missing support.

A fresh unprimed visual reviewer inspected the complete atlas and enlarged panel/footer crops. Initial findings were unclear neuron-count units and invisible local taps; explicit units and separate tap insets address them. The second pass found inset-number semantics and coincident-tap ambiguity; final inset headings state the tap count and that labels are sample IDs. It confirmed no clipping/overlap and distinguishable coverage colors. The author inspected the final full layout. Remaining presentation limits are small inset labels and prose-only orientation, with substantial white space deliberately preserving every coverage cell. No cell is hidden behind an inset. This is a data atlas, not a browser retinal capture.

The original 1140×1120 atlas had quantized color entropy 2.101 bits, dominant-color share 0.666 and luminance range 219.179. Its low entropy comes from the white document background; the visible four populated hex grids establish that this is neither an empty capture nor a missing-data substitute. The final atlas is wider to keep tap detail separate from full coverage.

Ownership review: legacy directional export remains in `vision_map.py`; the opt-in retinal exporter owns registration, normalization, validation and its offline oracle. It reuses the source eligibility/path inventory from slice 04. No compatibility channel, new neuron, signed-response assumption, gain boost, physiology claim or behavioral tuning was introduced. Documentation explains the binding and model choices without changing shared plan or decision files.
