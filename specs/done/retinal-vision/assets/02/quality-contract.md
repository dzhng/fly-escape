# Acquisition quality target

Freeze before comparing the profile captures. Judge recognizable lateral colored landmarks, a real doorway gap at two heights, and furniture boundaries. Texture detail is outside this gate.

The three 0.25 m colored cubes are at (2,0.35,2), (2,0.35,3), and (2,0.9,2.5) metres. The observation origin is (1,0.45,2.5): the lower targets subtend approximately 12–13 degrees, with red on physical left and blue on physical right. The upper green target tests height. Require at least one retained cell identifying each target in its appropriate eye; the dominant linear-RGB channel must be at least 1.5 times each other channel and at least 32/255. The blue sky must not be mistaken for the more saturated blue cube. A +0.15 rad heading change must move the target's sampled location or coverage reproducibly, with exact bytes restored when the pose returns.

The doorway fixture is the authored gap at x=4.8, z=2.1..3.0, viewed from (3.8,height,2.55). Its 0.9 m opening subtends approximately 48 degrees. Aim the physical left eye at the gap by setting body heading to π/2−0.47 radians; inspect both 0.01 m floor and 1.1 m flight heights. The initial heading-zero capture put the doorway at the inner field edges and cannot judge both jambs; retain it as a framing failure, not a failed resolution. Require separate vertical opening boundaries and visible interior beyond the gap in the selected mosaic, using the highest tested profile as a detail reference. Known furniture should remain distinguishable from the floor. Do not grade the surrounding workbench UI or use pixel identity as the quality criterion.

After the user rejected the first low-resolution matrix, compare 64/128/256 rasters at 721 cells and 217/469/721 cells at 128×128. Choose a profile that preserves these features and meets the revised time/storage contracts. Store failed candidates as evidence. Color bytes and deterministic return-to-pose are machine checks; doorway and furniture readability also require unprimed inspection of the eye interiors.

The physical fixture includes opaque authored door trim and a blue backing target beyond the opening. A separate opaque blocker fills the actual gap; every retained target-blue cell must disappear at both heights. This distinguishes seeing through the opening from a blue patch painted on the wall. It supplements, rather than replaces, visible separation of the opening boundaries.
