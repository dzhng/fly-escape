# Acquisition review

The original low-resolution selection failed. An unprimed reviewer inspected the full workbench, both framing attempts, all 56 final eye images and all enlarged candidate/reference pairs. No profile passed every gate: doorway boundaries and interior remained ambiguous, particularly at flight height. Furniture separated as a dark mass; higher sample counts retained the small colored targets more clearly. The user independently said the resolution was far too low to make out useful structure. Treat this as a failed quality gate, not a cosmetic panel-size request.

The first heading-zero doorway capture put the opening at the edge of the eye field. A second, eye-aligned pose still did not establish a readable opening into the beige room. Preserve both observations; investigate a controlled target behind the unchanged real doorway, with an unpooled camera comparison. Higher capture and retained neural sample counts are now the active variable.

Independent code review found that selecting 16×16 with 127 cells could replace the working capture with an unsupported layout. The workbench now disables incompatible options and validates a proposed projection before retiring the old capture. The worker also validates first. Focused behavior checks remain part of closeout.
