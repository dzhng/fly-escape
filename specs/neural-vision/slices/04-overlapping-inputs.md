# 4. Replace the sparse hard sectors with overlapping receptive fields

Both original motor pilots and the fixed DNp03 diagnostic failed. Do not modify the decoder: the conditional permission in slice3 never activated. The original source populations and signed graph remain valid anatomical candidates, but their hard, exclusive sectors reserve total current according to the sparsest endpoint sector (6 cells for Tm2,13 for Tm20). This severely limits all directions. The input cells respond, so this is a modeled projection revision, not evidence that the graph lacks visual neurons or a license to command movement.

Run one further bounded projection experiment. Keep the same annotated cells, source hex1 ranges, modeled eye-angle equation, graph, readouts, neural/body parameters and brightness compression. Replace exclusive sector membership with overlapping cosine receptive fields. This is a modeling assumption, not measured retinal tuning. For cell i with the existing modeled preferred angle and direction b, raw weight is `max(0,cos(angle_i-angle_b))`, with values below1e-12 treated as zero. Divide each column by its sum, then divide the whole matrix by its largest row sum. Thus every direction has equal summed weight, every cell's total weight is at most1, and rare endpoint sectors no longer set the entire input budget. Record the resulting effective population per direction.

The production map hard-cuts to unique entries `{index,weights:[f64;8]}` plus the existing identity/family/registration fields. Remove the old bin/normalization runtime representation; preserve its frozen artifacts and failed results as historical evidence. `Graph` validates finite nonnegative weights, nonempty/equal column sums, each row sum≤1, unique existing non-motor indices, and matching graph/annotation provenance. The exporter owns this single projection. No second runtime map format or per-tick annotation work.

Current for each neuron is `gain * sum_b(weights[b] * brightness[b]/(brightness[b]+0.5))`, clamped to[0,gain] only for rounding safety. This keeps the existing gain ceiling3 and all eight directions active. Blocked/odor/camera/exit data remain excluded. Reuse the same exact optical fixtures and analysis; no desired sign, body-gain change or new neuron family.

Freeze this protocol before execution: Tm2 and Tm20, **gain3 only**, pilot seeds1..6, original60warm/100measure, original12conditions. Choose by the original real-lamp motor |t|/95%CI gate, ties smaller population; freeze the selected axis. This is a new projection pilot, not an extension of the old seed search. Do not claim that equal total current removes nonlinear population or anatomical asymmetries. If both fail, preserve the result and reassess the model rather than silently opening more gains or changing decoding.

If a candidate passes, run held-out seeds100..129 and the original direction/intensity/occlusion/silencing panel with that exact frozen map and primary axis. Retain the original body configuration and readouts. The motion panel records walking and flying separately. Before publishing, regenerate selected metadata, require graph bytes unchanged, update consumer tests/probes to the single new map shape, verify cost and actual browser playback, and complete the original closeout gates. Original hard-sector maps remain historical only; mark their instructions superseded without deleting the negative evidence.
