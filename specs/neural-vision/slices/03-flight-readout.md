# 3. Resolve the failed motor gate at the readout seam

The frozen one-bin input pilot failed all six motor confidence gates. Do not select its strongest failed candidate, run held-out seeds, publish a winner, or revise input angles to fit the result. Input firing rose substantially and many relay voltage differences were observed; these descriptive intervals are not corrected for multiplicity. The failure therefore does not establish absence of neural vision. The current walking decoder reads olfactory-labelled DNs, and the flight decoder averages a mixed flight population.

A bounded diagnostic now tests one independently motivated pair: source-annotated MaleCNS **DNp03**, soma L/R (current body IDs 10752/10989). Actual retained two-hop paths from both candidate input families reach it. [Primary electrophysiology](https://www.sciencedirect.com/science/article/pii/S0960982224016415) associates DNp03 with visually elicited flight saccades; this supports a readout candidate, not a claim that modeled steady brightness is natural looming or that another specimen's IDs apply.

Before changing production decoding, run exactly Tm2 and Tm20 at gain2, the same pilot seeds1..6, fixed-pose rate1 lamps left/right,60warm+100measure. Record each DNp03 cell's mean voltage/firing, right-minus-left voltage difference (also retain the negated left-minus-right value for clarity) and the existing motor outputs. Select DNp03 by source type/side, never by whichever neuron shows the biggest light effect. This is an exploratory readout diagnostic, not a passed confirmatory result. Save annotation/source IDs and actual path evidence beside every result.

If neither candidate has a DNp03 voltage difference whose paired95%CI excludes zero, stop this revision and reslice from that negative evidence. Do not broaden a neuron search automatically. If one or both pass, freeze the larger absolute paired t candidate (ties smaller input population) at gain2, and revise the output seam explicitly: a new annotation-derived DNp03 L/R pathway supplies the visual component of **flight_turn** using the existing0.5 voltage-difference scale; all other body/neural parameters and walking/olfactory decoding stay unchanged. This must still be a readout of actual simulated neurons, with no sensory value/angle/exit target reaching the decoder. The broad flight groups may continue owning thrust. Source verification and motor-input exclusion include the new pair.

Then rerun the pilot's mirrored real-lamp motor check with that decoder, freeze flightTurn as the single primary axis, and run the original held-out direction/intensity/occlusion/input-silencing panel. Do not borrow the exploratory DNp03 p-value as confirmation. Closed-loop evidence must include flying initial mode because the functional hypothesis concerns flight; retain walking as a comparison, not an assumed visual-turning claim. Preserve the exact production body configuration. Existing campaign regression must show that the readout change does not break playable rounds; record any changed outcomes honestly. No puzzle content is specified here.

This revision supersedes the slice2 ban on any readout change only if the diagnostic gate passes. It does not authorize changing neural dynamics, input maps, signed synapses, body gains, exit/odor forces, or selecting a wanted turn sign. Keep the first failed pilot permanently and distinguish every subsequent protocol/version in its evidence.
