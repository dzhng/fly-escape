# 5. Confirm neural propagation separately from movement

All hard-sector, DNp03 and overlapping-input motor pilots failed. Preserve those results as failures. The original objective asks for controlled, causal neural responses; this spec mistakenly made significant turning a prerequisite. This explicit revision separates the neural gate from the behavioral evidence needed for the lighting puzzle. It does not turn any failed motor experiment into a pass.

Freeze the smaller audited population, Tm2, at gain3 using the overlapping projection. This choice depends on population size, not exploratory relay responses. Use corrected map hash `c0ac5957037012b486cc7300ccad56015fd3215ad4c764ce4ba0b27c82a262da`. Freeze the graph, annotation, adapter and probe identities plus exact endpoint indices before execution. Seeds100..129 remain untouched; run them once with the existing60warm/100measure fixed-pose, paired streams and unchanged neural/body parameters. No additional families, gains, seed search or motor decoding changes.

The fixed endpoint is the entire sorted union of non-input, non-motor cells on frozen audited relay/readout paths, verified against actual graph edges. This is a downstream observation population, not a pure anatomical cell class or proof of a unique intervening route. Save indices and body identities before observing results. Every member participates in statistical correction; do not select a relay by previous effect size.

For every endpoint cell, calculate paired seed differences in its mean voltage for seven contrasts: opposing basis0−4,1−5,2−6,3−7; real lamp-left−lamp-right at rate1; high−low intensity at rates2−0.5 separately on left and right. For R endpoint cells, use simultaneous Bonferroni95% intervals across all7R comparisons: mean ± t(29,1−0.05/(2*7R))*sampleSD/sqrt(30). Each of the seven contrasts must have at least one corrected interval excluding zero. Report all results, including zero-variance differences. This supports those opposing distinctions, not complete eight-way behavioral decoding. Use the existing scientific Python runtime for the quantile/analysis; no production dependency. Spikes and motor effects remain descriptive.

Optical gates: wall and furniture brightness must equal dark with positive blocked telemetry, and full neural trajectory hashes must equal dark in every seed. Matching opening must reproduce the visible right lamp. Injected total current must strictly increase over rates0.5,1,2 on each side. Add input-silenced versions of every basis and low/high lamp condition; every such neural trajectory must equal dark-inputs. Keep rate1 silencing and require its same equality. No tolerance may be introduced after observing results. A matched-count zero-outgoing sham must preserve the unsilenced left/right downstream voltage and spike vectors exactly; this only checks silencing machinery, not specificity against arbitrary connected neurons.

Use a separate neural-confirmation protocol and output directory, preserving failed pilots. Freeze before execution; passing requires all corrected contrasts and optical/causal gates. If it fails, report the remaining neural limitation instead of searching more parameters. Complete the original worker, playback, resource and integration gates if it passes. Run the bounded production-body walking/flying motion panel and report it descriptively. Objective4 remains unresolved unless usable light-driven behavior is demonstrated; no lighting-puzzle success claim follows from this neural gate.
