# Final review corrections

The whole-feature review inspected the implementation from 88813fe5 and found three concrete failures: a GPU fence already completed during suspension could be rejected on wake; a cold catalog setup could wait forever before the attempt watchdog started; and switching the workbench back from its one-fly archive could leave campaign props behind the independent fixture’s eye images.

The capture owner now accepts completed fences before considering its deadline and excludes explicit page suspension from active waiting time. The existing client watchdog owns setup recovery too, and rejects setup during an active attempt without resetting that attempt’s watchdog. The optical fixture restores its worker-returned appearance before showing the capture. The one-fly view gets its catalog from its own producer and ignores late setup completion after disposal.

Validation: 147 client/renderer/web tests pass, typecheck passes, the complete tick/cancel/map-retry gate passes, and the one-fly browser/native comparison preserves all integers/metadata and floating values within its declared tolerance. The added workbench check compares complete context PNG bytes before and after the fixture → one-fly → fixture sequence. Regression mutation checks fail when setup monitoring, fence-completion ordering, or fixture restoration is removed, then pass after restoration. The fault suite also exercises a pending fence across six simulated seconds of explicit suspension.

A second independent static Codex review of the final source and resource harness found no actionable defects. It ran no tests or builds. Shape review keeps setup/watchdog ownership in AttemptClient, GPU progress ownership in RetinaCapture and fixture appearance in its CaptureReady snapshot. No new worker, timer manager, alternate world authoring or fallback was introduced. The source correction is 39 net production-source lines including comments; tests exercise outcomes at the client/browser boundaries.
