# Authored scale review

Target: the selected fly stays recognizable at close zoom and its yellow ring identifies one body in the unchanged dense diagnostic spawn. The GLB owns world size; follow distance derives from bounds.

The source now exports a maximum span of 0.31056 world units against 0.4-unit diagnostic spacing. Blender MCP regenerated the authoring scripts in a separate directory; the source and committed asset have identical finite bounds, grounded contact and four clip durations ([measurements](scale-source.json)). The numeric suffix fix also permits regeneration after Blender has already loaded more than nine copies. No body radius, spawn position, neural behavior or camera constant changed.

[Camera checks](scaled-camera/checks.json) pass shared selection, centering, zoom, pan, overview and keyboard/card behavior. [Workbench checks](scaled-workbench/checks.json) pass invalid replacement and five replacements with stable GPU resources. Renderer tests pass all five tests and 39 assertions. The browser uses production builds and the actual GLB.

[Pixel/edge comparisons](scale-diff/visual-parity-diff.json) cover all five production camera states at the same viewport, seed and tick-zero playback cursor. The bounds-derived close camera intentionally moves closer after the asset shrinks; this is the framing contract being tested. All pairs differ. Default-follow distance from the previous image is 0.08625; this locates a real change, not an acceptance score. The candidate is less wrong at follow/close because the selected body and neighboring bodies are now separate while screen detail remains available.

A fresh reviewer inspected every candidate PNG: eleven full frames and three enlarged crops. No detached limbs, broken wing silhouettes, missing selected model or severe shadow artifacts were found; the selected fly is fully framed and the ring clearly surrounds it at close/follow distance. Far-side legs can be naturally occluded, and the ring has only medium contrast against the current pale floor. The whole-house overview still loses the tiny selected marker; the workbench overview demonstrates world scale rather than anatomy. The playback roster also ends in a clipped row. These remaining issues belong to camera marker readability (06), house palette (11) and the in-progress science panel (13). They are recorded, not dismissed as a fully accepted game.

Shape/diff/docs review: scale remains in the authored asset, with no per-fixture correction or duplicated runtime owner. The change adds three production source lines net and an output-directory override in the existing browser harness; generated assets and review evidence dominate the diff. The remaining camera gate and human review are open, so 07's dependency acceptance is still open. Motion sampling is checked separately in 08.
