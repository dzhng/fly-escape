# Timed-round integration

The explicit timed life policy preserves physical contact, neural control and hazards while disabling energy transitions in campaign play. Finite-energy diagnostics remain available separately. The native workspace suite and focused body/attempt regressions pass, including a paired finite/timed outcome test and physical apple landing without feeding. The generated schema, TypeScript check and WASM/web/asset-lab builds pass. Topology tests now consume the actual authored campaign modules rather than obsolete map copies.

Production Chrome, Apple M5 Pro hardware renderer, first level seed105: the full3000-tick round completed with9escaped,11timed out and zero starvation. Fast used5×, with zero underruns and17ms frame-interval p95. Initial wait was83.99seconds; this is a failed startup-performance target, not release acceptance. This run used the original retry assertions before the additional countdown/two-button assertions were added.

Second level seed11716541513791412824: the default90-second startup observation timed out. The captured report was still buffering at tick1880, with no simulation error and continuing production. An explicitly extended observation follows separately; extending the observation does not relax the default gate or accept startup performance.

Raw reports retain build/graph/seed identities and memory measurements. Memory fields overlap (WASM already includes brains and graph); do not sum them blindly. One attempt per house does not establish puzzle balance, multi-retry stability or platform acceptance. Current neural initialization is unchanged and cold. Placement comparisons in the timed regime are still required.

With the diagnostic startup limit explicitly extended to240seconds, the same second-level seed completed at3000ticks: all20timed out, no starvation or hazards. Initial wait98.37seconds, production145.12seconds, zero underruns and17ms frame p95. The harness verified the two named speed choices, absent starvation counter and countdown reaching0:00. The captured final screen was inspected for those functional states. A native pilot preparation/build was allowed concurrently, so these timing numbers are observations rather than isolated comparative benchmarks. Zero escapes with no player placements is a baseline observation, not proof that the level is unsolvable or balanced.

## Review disposition

The [independent Claude review](independent-review.md) found no core life-policy correctness defect. Root accepted a direct `ReserveModel::default()` instead of extracting a known variant with runtime unwraps, removed obsolete starvation-only calibration summaries and a stale benchmark energy description, and clarified the diagnostic energy model in About. Finite energy remains the Rust body default because it is used by existing finite-life diagnostics; campaign content requires an explicit serialized life policy, and both authored campaigns select timed. The speculative future third-level default concern does not affect the two-level runtime. The recommendation to hide other zero counters was rejected: native fixed/placed hazards are resolved independently of the legacy zappers array, and that visual change is outside this pass. The missing new calibration inputs are real pending balance work, not grounds to retain the obsolete map fixture. Local naming feedback required no behavior change.

The latest user scoring clarification adds a tested distinction: a timed round with only hazard victims retains its horizon, whereas all escaped ends early. Finite-life diagnostic termination remains unchanged. Native attempt regressions cover both cases. Three-star thresholds are still provisional; the target is roughly10% of players, which cannot be verified by a percentage of random seeds. A separate Perfect tier remains undecided pending attainability evidence.
