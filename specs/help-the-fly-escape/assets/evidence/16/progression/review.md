# Campaign seam preparation

The release registry is deliberately empty pending accepted content. Root must integrate it alongside accepted first-level data; the diagnostic setup remains at /lab/setup and is not a fallback release puzzle. No campaign levels or cue effectiveness are accepted by this checkpoint.

The browser-only fixture mounts the same Campaign and SetupGame components with three explicitly test-authored LevelDefs derived from diagnostic geometry. It never enters the production registry. The test proves sequential lock display, original-inventory filtering, exhausted stock retained at zero, per-level setup restoration across switching/reload, reset relocking, attempt navigation locking, and playable denied storage. A real one-tick attempt verifies zero-star completion does not unlock the next level. One-star progression is supplied as persisted test input, not claimed as a neural simulation win; the focused unit test connects awardResult to sequential unlock. Worse results preserve earlier best stars.

The core catalog command uses the existing WASM tool_catalog function, with no WASM/core edits. Parent-owned progress survives editor remounts. Stable authored content props prevent progress updates from recreating the world. Worker lifetime follows the selected editor, and navigation is unavailable during playback. There is no second mutable unlock store.

Fresh image review inspected all three full frames and six navigation/control crops, with prior renderer review context but no source inspection. Current level border/title, locked labels, stars, inventory and storage warning were readable; panels fit the viewport. No blocking visible defect was found. Disabled exhausted stock is faint but legible. Keyboard behavior is not inferred from still images. The implementer's self-review discovered and fixed missing production CSS in the test entry and full-height overflow below navigation before these final captures.

TypeScript, production build and three focused progress/client tests passed. The built release route presents its explicit unavailable state and the built diagnostic setup independently reaches ready. The actual browser progression harness and delayed placement-commit regression passed. Fixture harnesses that formerly targeted / now explicitly target /lab/setup; no assertion was removed. The delayed-commit harness now requires an existing enabled Run button, rather than accepting its absence during fixture loading. Local codex review could not run because its installed version rejects the selected model; root source review remains the independent review route. Campaign acceptance, authored topology/holdout gates and final production content remain open.

The older full setup workflow remains an unresolved gate: one development run timed out locating Replay after completion; another timed out waiting 240 seconds for the second attempt to complete. The dev tree received subsequent edits while the latter ran, so HMR could invalidate it; these are not sufficient evidence of a production defect. Do not report the full setup regression green. A frozen built-route rerun should follow accepted content integration. The focused campaign test completed successfully with final content-prop and shell sizing changes. The unlock unit test rejects a deliberate zero-star-unlock mutation and passes after restoration.
