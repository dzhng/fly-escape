# Selected-fly presentation and contact evidence

The roster owns compact model previews; one selected details section owns the brain, connectivity, all-group traces and explanations. Previews consume the world's sampled transforms, animation phases and camera orientation through the playback render loop. One atlas renderer serves the list, with 2D canvases for its tiles. Walking wings are stationary in the actual GLB (one unique rotation per wing in Walk; eight in Fly).

Visible walls are thicker than the simulation's wall boundaries. A fly's enlarged display envelope now ghosts an intersecting wall, including its base, without moving the recorded body. The matching kitchen fixture exposes a fly formerly obscured by the wall: 314,402 changed pixels airborne, 255,534 at floor height. Independent visual review found the fly visible and unclipped. Seeing grass through the ghost wall is intentional transparency; it makes wall depth less obvious. Wall-adjacent is not wall-attached: the airborne fixture keeps its horizontal attitude. The low clipping plane avoids coplanar floor noise.

Camera compensation now follows a continuous power curve: zooming closer gently increases apparent fly size; maximum world enlargement remains capped. Repeated wheel input at the close limit yields identical screenshots. Departures are deterministic presentation sampled from time since recorded escape: outward motion, climb and shrink, then hidden at eight simulated seconds. Camera tracking retains the recorded exit anchor. The result and simulation remain unchanged. Departures use playback time, so pause and the end of the playback horizon stop their animation too.

Verification: typecheck, production web build, 70 renderer tests, compact science-panel browser checks, camera browser checks, and exact PNG pause/reverse-seek checks passed. Runtime instrumentation on the normal playback component verified that all 20 preview inputs equal the moving world poses and camera orientation; the retained script uses the development playback route. Science checks cover 20 entries, one details section, one brain, two traces, selection and bounded tooltips. Fresh review found legible roster/tooltips and stable zoom framing; overlaid chart lines remain dense. Maximum close zoom still emphasizes recognizing the fly over anatomy inspection. Departure captures establish visible departure and disappearance, not animation smoothness by themselves. Scripts retain the deterministic fixture and exact-pose instrumentation used for inspection.

The six-fixture native contact measurement is separate from the visual wall correction. Dense and halved sampling found worst penetration 1.715 µm and root deviation 2.389 µm, under 3 µm. This is bounded native evidence, not a WASM/archive round-trip proof.

Open overall gates remain open. The existing 30-seed native tuning process continues unchanged. A separate ten-attempt production resource run stopped after attempt one because frame p95 exceeded 25 ms under concurrent calibration/contact work; it is not a passing ten-attempt result and predates this panel refactor. Actual Safari manual smoke observed placement, playback and pause, not a complete release acceptance run. No star thresholds or neural parameters changed here.
