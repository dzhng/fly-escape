# Shared house renderer checkpoint

The geometry target is an intact five-room footprint, a pantry with one physical opening, a fly visibly crossing that opening, and walls that reveal a followed fly without changing collision. Neutral materials deliberately keep palette and lighting out of this checkpoint.

The asset workbench exposes `?fixture=house` through its “Five-room house” link. Both modular assets map directly to the shared geometry payload. Door probes subtract wall coverage from shared room boundaries; there is no handwritten TypeScript doorway map. Local replacement validates native bounds/pivots, shares resources across placements, and releases the previous parts. A failed or superseded load retains the displayed part.

[Browser verification](../../../../../tests/browser/house.mjs) exercises real controls for overview, pantry, doorway crossing and cutaway, repeats wall/floor replacement, and rejects a floor uploaded as a wall. [Results](browser/report.json) retain equal GPU geometry/texture counts before and after replacement and no browser errors. The existing fly asset workbench browser regression also passes. Renderer tests pin open passages, nested-segment cutaway/restoration and one-time resource disposal. Disabling recursive ray intersection makes the cutaway test fail, then restoration passes. Typecheck and the asset-lab production build pass. No core or WASM source/build was changed.

[Whole layout](browser/overview.png), [door threshold](browser/pantry-threshold.png), and [wall cutaway](browser/pantry-cutaway.png) provide the main review surfaces; all captured states have matching enlarged crops. [Pixel telemetry](browser/pixels.json) deliberately measures the world separately from UI. The large uniform areas (dominant gray shares around 0.8–0.9) reflect a sparse greybox; overview still contains the complete footprint and all door gaps, while close views place the fly centrally. The prior kit renders show isolated pieces rather than a comparable layout, so no misleading paired image score is claimed.

Adversarial inspection: the pantry-center close view is mostly empty floor and cannot establish room topology by itself; the overview is required. Cutaway at the corner removes both intervening wall segments and can read like an open platform; the overview and physical geometry fixture establish the walls still exist. Fine diagonal shading is visible on wall faces at close scale; final lighting acceptance remains open. The first checkpoint used the older floor-height airborne ring; the final candidate incorporates root commit 87f5ac1's aerial selection-ring correction. Fresh independent review inspected all six full screenshots and all six crops. It confirmed readable five-room topology, intact gaps/joins and no fly intersection at the threshold. It flagged the corner cutaway losing enclosure (high confidence), close views relying on overview/UI for room identity (high), and stepped/diagonal wall-shadow banding (medium). The enclosure finding motivates the follow-up below. Close-view framing and shadow banding remain acceptance limits for the relevant camera/lighting work; no palette or lighting change is introduced here. CLI independent code review also failed at startup: the configured model requires a newer CLI; no review findings were produced.

Shape review centralizes room mesh ownership and deletes the old inline wall builder. Diff review verified local replacement generations and pagehide invalidation, and removed a cross-package private resource import. Root independently reviewed the functional source and found no code defect. Documentation keeps the geometry and asset contract in their existing owners. This checkpoint is prepared for integration, not full slice acceptance: the existing Geometry wire contract has no solid-prop footprints. A follow-up core contract and collision fixture must precede solid prop meshes.

## Enclosure cue follow-up

The [follow-up captures](enclosure/pantry-cutaway.png) address the high-confidence enclosure finding with a low base along each occluding segment. The same authored wall instance is reduced to one tenth of its height; it keeps the footprint/corner visible without adding geometry or changing collision. Full height is restored before each ray query and when follow ends. [Restored overview](enclosure/restored-overview.png) and the repeated-query renderer test cover that restoration. Removing the matrix refresh produces the expected red test, then restoration passes. [Comparison telemetry](enclosure/comparison.json) pairs identical views with the first checkpoint; no palette or lighting values changed. The first low-base follow-up still showed a notch at the outside corner and an occluded selection in corner Overview. The final candidate below addresses both findings.


## Joined walls and selected-fly visibility

The [final candidate](selected-visibility/pantry-cutaway.png) joins only wall ends that share an authoritative endpoint with a noncollinear segment, extending each joined visual end by half the kit thickness. Lone ends keep their exact extent. A corner ray test fails before the join fix and passes afterward; the independent open-end probe and all four house doorway probes remain clear. No extra meshes, physics or handwritten layout are introduced.

The [actual corner Overview](selected-visibility/corner-overview.png) now retains the selected fly's location through the same low-base cutaway. Overview releases follow rather than selection. A selected center inside the drawable viewport may lower occluding walls in any camera mode; offscreen selections restore all walls. Browser controls exercise that offscreen pan as well as [the unobstructed center before](selected-visibility/pantry-overview-before.png) and [after](selected-visibility/restored-overview.png). Those same-location Overview frames are byte-identical. A pixel probe finds yellow marker pixels at the actual corner; [a tight marker crop](selected-visibility/corner-marker-crop.png) comes from the renderer's projected anchor. The [comparison](selected-visibility/comparison.json) records world/full differences separately. The [final independent review](selected-visibility/critique.md) found the notch resolved, the marker visible in both Overview cases, and no further blocking geometry/visibility issue in the supplied samples. Correct partial occlusion of the ring/legs remains visible at the low base. Camera-button active-state indication and close-up wall/shadow banding remain separate UI/lighting notes. Source typecheck, renderer tests, house browser checks, the asset production build and ordinary fly-workbench regression pass. Root coordinates the non-blocking human Preview checkpoint after integration.

## Solid geometry seam

The previously open solid requirement now has a [prepared authoritative footprint checkpoint](solid/review.md). Its core/worker changes belong to that follow-up; the earlier renderer checkpoint retained the previous wire contract. Final integrated and human review remain open.
