# Solid footprint checkpoint

The target is a visible authored obstacle whose floor footprint is the same obstacle used by body sweeps, sensory visibility and placement validation. A stopped fly must remain readable, while a full-house view must show the obstacle's height and location.

Geometry now owns required solid rectangles and presentation heights. Its existing obstacle traversal feeds body occupancy, sweeps and line of sight; field cells and fixed source/contact centers exclude solid interiors. Placement and spawn consumers already use the shared occupancy query. Empty fixtures explicitly supply an empty array. The five-room JSON remains the only house layout; the pantry solid leaves the original doorway route open.

[Blender roundtrip](blender-roundtrip.json) records the separate authored scene and native unit bounds. The solid mesh has 28 triangles and one material; its top-only bevel preserves the floor footprint. The original Blender scenes were retained. Runtime mapping derives all scale/position from Geometry. Local replacement accepts the kit pivot/bounds, shares resources and disposes replaced parts. Bounds checks cannot certify the internal shape of an arbitrary user-supplied mesh; the supplied authored block is explicitly checked against the collider, and replacement remains a developer workbench operation.

The core diagnostic uses the real body radius and Geometry sweep through a thin WASM worker. It never advances a brain or claims neural obstacle avoidance. One active query and one latest pending slider position bound work; cancel drops stale replies, fatal worker errors remain visible. The [browser report](final/report.json) proves that crossing stops at x=6.669999999, immediately before minX=6.75 minus radius 0.08, with blocked line of sight. The nearby detour reaches its requested endpoint with clear sight. Four replacements retain 44 GPU geometries and four textures with no browser errors.

The initial [full-height close candidate](browser/stopped.png) hid the fly and was rejected. The [final stopped pose](final/stopped.png) retains the exact obstacle footprint as a low base while exposing head/body/wings. This generalizes the existing selected-fly policy to solid owners; no collider or field changes with camera visibility. The [full-height overview](final/overview.png) and renderer restoration test cover the other state. Ring and leg portions retain normal depth occlusion. [Pixel telemetry](final/pixels.json) compares identical viewport/camera/static contact poses: grayscale MAE 6.97, RMSE 26.50, 9.18% of pixels differ by more than 16. Those values locate the changed occluder, not determine acceptance. Every captured candidate state has both world and controls crops. All prior candidates remain present.

Native sim tests pass, including unchanged four-door connectivity, stopped/open paths, excluded field cells and tool placement, rejected spawn and malformed solid geometry. The collision regression was observed red before the shared obstacle query was extended, then green. Renderer tests pass (11) including authored solid bounds, recursive cutaway/repeat/restore and disposal. Client tests pass (15), generated typecheck passes, WASM and asset-workbench builds pass, and the ordinary fly-workbench browser regression passes. Vite reports its existing large renderer bundle advisory. [Independent visual review](final/critique.md) inspected all five full frames and ten crops and found no blocking geometry/readability defect. It identified weak solid/floor contrast for slice 11. Root independently reviewed environment/fields/placement/body/attempt, house loading/cutaway and probe client/worker and found no blocking source issue. Root owns the merged-view checks and integrated human Preview checkpoint. Palette and shadow banding remain slices 11/12.

Shape review retains one geometry owner and generalizes the existing cutaway helper instead of adding a solid-specific visibility system. Diff review corrected source-root scale multiplication and a fatal-worker busy-state dead end. Docs place the wire invariant in CONTRACTS and checkpoint intent in slice 10. No new dependencies, scene clearing, campaign content or steering controller were added.
