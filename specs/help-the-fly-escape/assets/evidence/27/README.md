# Sensitivity candidates — confirmation pending

Diagnostic builds change only the relative contrast multiplier in the canonical sensory adapter. Production remains at5%. The0.1% candidate misses all anatomical odor inputs in the three-seed comparison. The0.01% candidate restores expected signed odor turn differences in all24 anatomical active-versus-neutral comparisons (two pathways, two initial phases, three seeds, two mirrors). All matched pathway-silenced motor/body trajectories remain exactly neutral.

At0.01%, lamp/shade paired median turns retain the expected opposite directions at both initial phases. One of24 individual visual comparisons has the opposite sign; this small comparison is evidence for confirmation, not a deterministic claim. Visual ablation removes the response exactly against separately silenced neutral controls. The visual sampling pair remains a planar bilateral approximation at antenna coordinates, not an eye-optics model.

Three-seed compact results and compressed raw trajectories are retained here, with exact odor adapter source snapshots. Earlier odor probe condition strings still say5%; each summary explicitly overrides that stale description with the actual fingerprinted diagnostic cutoff. The extended vision probe records the adapter hash directly. No source graph, current gain, LIF dynamics, field transport or production physical default changes.

30-seed odor and vision confirmation runs are live from `/tmp/fly-continuous-fields`, candidate0.0001, using the extended canonical physical_sampling example. Outputs: `/tmp/sensitivity-odor-confirm30.json` and `/tmp/sensitivity-vision-confirm30.json`; logs share those stems. Tool sessions11442 and80353 respectively. Verify the existing process handles before restarting. Production adoption and27 acceptance remain pending their result and review.

The existing sensory seam test now pins uniform nonzero and sub-intensity-floor neutrality. It passes, fails when detection is forcibly enabled, and returns green when restored. This test addition changes no production behavior.
