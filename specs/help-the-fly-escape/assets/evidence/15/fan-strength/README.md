# Useful fan strength

The catalog fan now supplies 3 m/s at its source, retaining the existing wall-blocked jet,3 mreach and lateral/longitudinal falloff. Neural tuning, life costs and movement decoding are unchanged. This is an authored game setting, not a measurement of a particular appliance.

On the current domestic first-level geometry before the desk/bed placement, three diagnostic seeds yield 17,18,13 reference escapes versus 3,1,0 with no tools. The second-level starting strategy still yields only 1 escape on each seed versus 0 empty; its route and food remain unresolved. These are unfrozen diagnostics, not campaign acceptance.

The integrated real Chrome/WASM run with the new study and bed placements gives 17 escapes and 3 stars on seed 1000. Starting with no earned stars, it saves the result, returns to editing and unlocks/selects level two. No browser errors, initial buffering 1.23 s, no buffer underruns and 17 ms p 95 sampled frame interval were observed. The run checks computation and earned progression, not yet the full distribution or platform gate.

Production contact fixtures now consume fan strength from the catalog rather than a duplicated 0.5 value. All native-hull cases—including aligned apple contact—advance within 512 queries/129 knots with the existing penetration checks. The independent experimental hull still passes its fixed 0.5 m/s comparison. A 3 m/s attempt on that unused hull hit the knot cap; it remains unadopted and is not a production substitute. BodyWorld constructs only the native hull. Contact budgets, replay schema and numerical tolerances were not changed. Twenty body tests, five furniture/geometry tests and four placement tests pass, along with TypeScript and rebuilt WASM.
