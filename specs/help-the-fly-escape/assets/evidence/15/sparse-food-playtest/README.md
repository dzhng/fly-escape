# Sparse food placement in the authored house

The production browser accepts a single banana just beyond the bedroom doorway and completes the five-minute attempt with twenty real-connectome flies. Seed 15789670027162723101 produced one escape. The saved placement records the actual clicked coordinates; it is not interchangeable with rounded native probe coordinates.

Root inspected the setup and final screenshots. The replay completed with zero underruns; this capture ran alongside native experiments, with 45.8 seconds initial wait and 50 ms frame p95. It is evidence of the complete placement/attempt loop, not release performance acceptance, a demonstrated placement advantage, or a new visual acceptance claim. The final report records current graph, manifest, simulation and level identities.

The browser script is scratch evidence and uses the actual development route, UI controls and rendered projection. A fixed random seed makes this one attempt repeatable; the human game retains fresh seeds. Native sparse-placement comparisons belong alongside this capture once complete. No fan is present.

## Ten-minute native baseline

The same three seeds, current production graph/manifest and authored geometry were run for 600 seconds with no fan. The empty controls escape [5,5,4]; a banana near the final room entrance gives [2,2,5]; a banana just past the bedroom doorway gives [1,2,0]; food at both transitions gives [5,3,3]. None of these candidates establishes an advantage over empty placement. This is a small exploratory comparison, not a frozen tuning or holdout acceptance result.

These results predate the [contact-turn repair](../../20/contact-turn/README.md). A demonstrated safe-turn rejection can artificially extend object residence, so do not use this baseline to claim that neural food retention explains all observed dwell or to tune final stars. Preserve it for comparison after the contact correction.

The configuration files specify the exact legal placements. Raw archives include identities, per-fly residence and room occupancy. The existing [duration probe](../../32/food-strategy/probe.rs) ran each content at 6000 ticks; it uses the same simulator behavior at this horizon. Each A file includes both the empty and reference attempt for one seed; C and D include reference only, sharing A's matching empty baseline.
