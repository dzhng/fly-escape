# Neutral wall-sconce preparation

The [authored source](../../../../../../assets/house/sconce/README.md) proposes a compact wall fixture with a hollow bound-edge shade, curved arm and backplate. Its mounting orientation and native size are asset assumptions, not a runtime placement contract. This pass adds no catalogue entry, light emission, material treatment, collision or gameplay behavior.

The three primary full views (`three-quarter`, `front`, `rear`) use the shared neutral stage. Matching crops enlarge the attachment region at 2× without filtering. The supplemental `underside` full/crop exposes the socket and shade supports; only its diagnostic ground is hidden so the below-origin camera is not occluded. The failed ground-occluded framing remains in `rejected-view`. This is a new asset; there is no baseline shape comparison.

`roundtrip.json` records shared source/export envelope verification and authored closed-part checks. `integrity.json` records a separate exported-GLB import and native-file reload: 11 closed parts, 2,098 geometric vertices and 4,156 triangles, baked transforms, no nonmanifold or inconsistent edges, degenerate faces, or nonadjacent self-overlaps. Its 10 nm weld joins attribute-split vertices only in the temporary inspection mesh; the authored/exported geometry is unchanged. Different attached parts intentionally intersect; the self-overlap check applies within each part, not a boolean-unioned assembly. The one-off inspection script was kept outside the repository; source authoring retains the meaningful closed-mesh and shared export checks.

Fresh image-only review by `contact_root_critique` inspected all three fulls before crops, without sconce source/history: recognizable shaded wall fixture, continuous arm-to-plate join and coherent rim/plate, no visible preparation blocker (medium-high confidence). Initial views hid the inner fastening, prompting the supplemental underside capture. Supplemental full/crop review (same reviewer, prior context disclosed) confirmed the holder, bulb-like form and radial supports. One spoke looked possibly short of the shade in projection (medium confidence). Direct triangle-BVH inspection found 24, 26 and 24 intersections between the three supports and shade shell, confirming attachment in geometry while retaining the visual ambiguity. The small staggered studs/control remain visually ambiguous in neutral clay; they are decorative fixture details, not gameplay controls. Actual wall mounting and browser scale remain unverified. A studio ground is an asset display convention, not proposed installation height.

Author self-review is informed by implementation and is not the fresh review. It found no silhouette or attachment break in the primary/underside images. Shape/diff/docs closeout retained one geometry source and shared export/stage owners, and linked the house hub to the asset notes. The independent Codex CLI source review could not run: installed CLI rejects the configured gpt-6-astra model as requiring a newer version. Root integration reviewed the geometry source, static GLB structure, shared-helper ownership and three-quarter/underside views. The visible short-spoke ambiguity remains documented alongside the geometric attachment check; no new preparation blocker was found. Python syntax and whitespace checks pass. Image metrics locate contrast/detail only and do not establish visual acceptance.

A combined four-view banana/sconce Preview review was open from 12:14:19 to 12:19:22 UTC on 2026-09-07 while independent work continued. No user response arrived. Root retained the prepared shapes reversibly on the recorded source/visual evidence and closed Preview; this records an agent decision, not user approval or browser acceptance.
