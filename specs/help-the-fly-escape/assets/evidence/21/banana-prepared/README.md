# Neutral banana prepared checkpoint

Target: a recognizable single household banana with continuous curved skin, identifiable ends and plausible floor support. Medium household size is an authored assumption, not a calibrated biological input. The canonical shape rationale is in the [asset README](../../../../../../assets/food/banana/README.md).

The final asset is approximately 22.24 cm long, 8.14 cm across its curve and 4.18 cm thick. Blender MCP authored and rendered separate asset/staging scenes. Shared house helpers exported and reimported its true bounds; the native scene reload contains the same 4,762 mesh vertices. The baked GLB has 9,520 triangles. The topology validator reports zero nonmanifold/inconsistent edges, zero tiny faces and zero nonadjacent triangle BVH overlaps, positive volume and Euler characteristic 2. The unchanged contact baker accepts its triangle export. These establish a useful closed surface for further collision work, not runtime contact behavior.

All three final full frames and their 2× crops were inspected by the author and a separate reviewer. The reviewer had no project history/source on the first review, then reviewed the revision with prior-image context disclosed. Initial rings and ambiguous pointed/flattened terminals were actionable. The first rejected three fulls remain in `../banana-rejected`; the next three fulls and three crops remain in `../banana-intermediate`.

The final follow-up verdict was a coherent stylized banana, with no blocking surface continuity or grounding issue. Remaining visible refinements: somewhat flat stalk attachment and broad left shoulder (medium confidence, full and crop); small silhouette facets (high confidence, crop). Close shadows support grounding but cannot prove absence of penetration. The author agrees and does not claim photorealistic anatomy. Slice 22 material treatment cannot alone resolve these shape refinements.

The two comparable full-frame views retain identical staging/cameras. Pixel mismatch is 0.896% at three-quarter and 0.980% at top, confirming a real silhouette change. Grayscale scene contrast is intentionally low (three-quarter P95−P5 = 11), so this neutral authoring setup is not evidence of final gameplay contrast. The end camera was raised to remove the visible background strip and better expose the stem; it is excluded from quantitative before/after comparison. Every captured view is retained.

Review: one geometry owner, shared exporter/stage reused, no loader/runtime/catalogue changes, no apple/contact source changes. Root also reviewed the source and requested a durable validation contract and explicit temporary-weld documentation; both are now present. Source and documentation self-review found no unresolved code defect; `git diff --check` passed. Independent Codex CLI review could not run: the installed CLI rejects the configured gpt-6-astra model as requiring a newer version. Screenshot critique did run independently as described above.

Open gates: production browser replacement/disposal and room/camera composition, anatomical refinement for the photorealistic target, actual food support/contact and eventual catalogue adoption. No runtime route, physics, fly heading, neural inputs, level placement, house palette or lights changed. Slice 21 remains pending; this is banked asset preparation only.

Root integration review inspected the three final full views and agrees with the prepared-shape verdict and remaining stem/shoulder refinement. All eight banked asset/capture hashes matched, both Python sources compiled, and the integrated contact exporter reproduced the banked bake byte for byte. Preview opened at 11:14:22 UTC and closed at 11:22 UTC on 2026-09-07. No user response arrived during the review window; the reversible decision is to retain the preparation with the open gates above, not to infer user approval or final art acceptance.
