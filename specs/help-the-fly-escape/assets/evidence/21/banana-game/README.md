# Native bananas in the game

Both levels offer one apple and one banana, preserving two edible fruit placements. Each uses its authored native size, exact contact mesh and shared odor effect. Scent crumbs remain odor-only. The existing baked banana has 9,680 triangles; exact native identity bounds edible models by their baked cardinality while generic floor cues retain their 5,000-triangle cap. Shading seams share only exactly coincident positions through the same browser-safe canonicalizer used by the baker. Re-exported banana contact is byte-identical; no geometry, tolerance or material was changed.

Merged native body/food/placement gates pass (30 tests), including landing and feeding on the authored banana. Typecheck, all 43 renderer tests, WASM and production web builds pass. Both home levels load, release twenty mixed flies and return to setup. Actual second-level seed 1000 with apple and banana completes with four escapes and a rendered result/retry without browser errors.

The matched food-energy-off run also has four escapes, and no feeding events occur in either arm: food dependence remains open. A second study-side fruit arrangement has two escapes and no feeding; it is not adopted. The browser fruit run has no underruns, but an 11.24-second initial wait and 17 ms frame p95. A separate bounded performance investigation addresses unnecessary distant-food work before accepting that cost.

Adversarial visual review: the initial component image includes a diagnostic placeholder fly and is retained as an unaccepted framing; the final image removes it. The apple's pronounced grooves look stylized, and the fruit is tiny in the whole-house view. The final component confirms a recognizable curved yellow banana and red apple at native relative size, while the production image confirms real tool placement. These images verify integration, not final photorealistic materials. Fresh-agent quota was exhausted, so this written adversarial review is the skill fallback.
