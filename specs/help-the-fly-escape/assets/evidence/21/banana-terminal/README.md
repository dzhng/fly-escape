# Banana terminal anatomy

The cut stalk has an uneven oval collar and almost flush flat cut face; the opposite smaller terminal has an irregular recessed blossom scar. Both are part of the same closed skin. The existing profile authoring owns this geometry: its cross-section follows the stalk tangent and its ribs diminish into the solid stalk cross-section. No separate overlapping end meshes, runtime contact changes or material differences were added.

The overall envelope is 22.4037 × 8.1289 × 4.1783 cm. Relative to original preparation, length changes +1.660 mm (0.75%) and depth −0.099 mm; height is identical. These are local authored end changes, not scaling. Shared staging, neutral material, lights and the original three cameras remain frozen. A fourth reverse view exposes the blossom face directly; it has no baseline and is judged as additional anatomy evidence.

Existing validator passes native reload, export bounds, welded closed manifold topology, consistent winding, positive volume, nondegenerate triangles, Euler characteristic 2 and no nonadjacent BVH overlaps. The GLB contains 9,680 triangles, 4,842 geometrical vertices after the validator's temporary 10 nm weld. Contact bake has 5,001 vertices because flat cap normals split some GLB attribute vertices; it preserves the same triangle surface. No runtime registration follows from this bake.

All four full PNGs and four corresponding 2× crops are supplied for final review. The preceding six-view shape checkpoint is `../banana-refined`; the three fulls in `../banana-cap-rejected` retain the first cap candidate with excessive inset and a longer small terminal. `../banana-round-cap-rejected` retains all eight round-cap images; `../banana-faceted-cap-rejected` retains all eight uneven-cap images before the cut-face facet correction. Older shoulder trials remain in their existing evidence folders. Quantitative comparison uses only the three matched camera states/crops; pixel mismatch in fulls is 0.132% three-quarter, 0.131% end and 0.150% top, localizing the actual geometry change.

Source review keeps the shared exporter/staging and existing validator. Python syntax and diff checks pass. Codex CLI review was attempted but the installed CLI cannot use configured gpt-6-astra. No loader, generic framework or contact machinery was added. HANDOFF was not edited. Browser integration, actual moving food contact, and material treatment remain outside this shape pass.

Independent visual review began with no banana-image history on the preceding shoulder candidate; subsequent anatomy reviews explicitly disclosed that prior context. Every current full/crop and every captured cap variant was supplied. The reviewer identified manufactured round disks, then triangular cut-face shading; those findings drove the irregular outlines and planar large cut face. The final all-four-view recheck confirms the broad triangular artifact is gone, ends remain differentiated, and no new continuity, silhouette or grounding defect is visible. The author also inspected all final views/crops and agrees. The named terminal artifacts are resolved at neutral authoring scale; this does not claim completed photorealism. Remaining overall appearance limits are the broad smooth body and somewhat regular clustered blossom detail, to be judged with later surface treatment and actual browser framing. Actual contact precision is not inferred from soft shadows.
