# Prepared household shapes in the browser

The existing proportions workbench now inspects native window, sconce and plant GLBs through the same static loader used for catalogue house assets. The explicit WorldView inspection slot owns appearance resources only; it supplies no core occupancy, wall opening, emission or placement rule. The lab owns proposed review bounds and mounting transforms until a physical catalogue contract is accepted. Camera focus uses the shared owner; a named diagnostic mounting elevation supplements the ordinary RTS angle. These choices avoid classifying fixtures as stretched floor/solid templates or adding a second viewer.

The [browser harness](../../../../../../tests/browser/house-shapes.mjs) ran headless Chrome against a production asset-lab build from the isolated worktree. `before` contains unchanged room context/Overview; `rejected-occlusion` retains the first native-shape composition whose wider plant view crossed the room wall. Moving only the diagnostic plant inward fixed that inspection obstruction. `candidate` contains all fixed full frames and detail crops, including the visible invalid-replacement error; model/light/camera orientation inputs are unchanged. This verifies browser readability and replacement preparation, not complete slice 21 or campaign adoption.

`candidate/report.json` records native and placed bounds, unit scales and +Z front vectors; window/sconce backs meet the existing wall's interior plane and the plant's base is at floor zero. Six cycles replacing all three files preserve exactly the same renderer statistics (119 geometries, 764 textures in this twenty-fly fixture). Invalid bounds preserve exact canvas pixels, a delayed initial response cannot overwrite a newer upload, and recorded tick 40→0→40 is pixel-identical. The recording contains twenty real-connectome bodies and 800 neural steps. Counts are bounded-run evidence, not a heap or performance certification. The authored resource-disposal unit checks and full renderer suite pass (36 tests), as do TypeScript and production build.

Material and lighting owners remain frozen: the prepared assets retain neutral authored materials under the current renderer lights, while the room retains its existing palette. The window therefore reads opaque, the sconce's arm is largely hidden by its shade in the ordinary RTS views, and the plant retains its known narrow leaf/tip styling. Authoring underside/shape evidence remains relevant; this fixture does not prove optics, realistic material response, leaf collision or sconce underside visibility. The old food in this proportions room is explicitly a proxy, not the final banana/food-contact model.

The first lifecycle run exposed same-filename upload suppression; clearing the consumed file input fixes iterative replacement and the frozen rerun passes. A disk-full interruption left a zero-byte screenshot, which was discarded; complete rejected geometry captures are retained. The independent Codex CLI source review was unavailable because the installed CLI rejects the configured model as requiring a newer version. Root reviewed the ownership seam; minor naming/import/label corrections were resolved. Implementer visual inspection is primed and is not the fresh critique.

Fresh browser critique by `contact_root_critique` inspected all eight fulls before eight crops and the rejected pair; prior authored-sconce/house context was disclosed, with no browser-source or expected verdict supplied. No shape blocker was found (medium confidence). Window and plant are upright, recognizable and unobscured. The sconce arm/backplate and mounting junction are hidden by the fixed elevated camera in both inspection views (high-confidence coverage limit, not a proven defect); the initial views did not establish that attachment visually. Plant ground shadow is weak, though no affirmative float/penetration is visible and measured native base is zero. The error is legible in the full frame and retains the model; its longer message moves lower camera controls below the scrollable sidebar fold. The world crop intentionally excludes error text. The generic wall-height note was replaced with a planter-specific ground statement. Neutral opaque window panes and deferred material/contact gates are not accepted as final art.

The canvas-only comparison uses identical 1120 × 894 pixel rectangles. Overview changes 1.744% of pixels under pixelmatch; grayscale >16 changes 1.775%, MAE 1.36544. Context is exactly identical, because its fly/fruit framing excludes all prepared household shapes. Accordingly, only Overview and the individual shape views establish new-shape visibility. `canvas-comparison.json` and `overview-canvas-diff.png` retain that distinction without counting sidebar changes as rendering proof. Final captures include the added mounting preset, followed by a fresh successful lifecycle run. The browser asserts exact ordinary Overview state and pixel-identical Follow rendering after returning from the mounting view.

The follow-up reviewer inspected `sconce-mounting` full then crop and refreshed plant fulls. The lower view resolves gross hardware occlusion: curved arm and backplate are readable, the arm meets the plate and continues beneath the shade, with no definite detached section or wall gap (medium-high confidence). The hidden back face/internal fastening still limits image-only contact certainty; measured placed bounds complement this view. No further shape/framing change was indicated. The planter copy is corrected. The extra mounting button puts Overview below this screenshot’s sidebar fold, with normal scrolling available. These findings close the bounded browser review fixture, not the remaining physical/material adoption gates.

Root integration reran the complete production build (including generated types and WASM), renderer tests, TypeScript and the browser replacement/seek harness. Running TypeScript concurrently with type generation initially read a partially generated file; the sequential rerun passes. The integrated browser report is retained as the [skeleton comparison baseline](../../17/skeleton-sharing/before-report.json.gz), avoiding another copy. Root inspected context, Overview and individual shape views including the lower mounting view. Preview was open2026-09-07 12:39:46–12:45:12UTC; no user response arrived. Retain the inspection composition reversibly on the documented bounds and visual evidence, without claiming user approval or furnished-game acceptance. The subsequent skeleton-resource fix retains identical pixels.
