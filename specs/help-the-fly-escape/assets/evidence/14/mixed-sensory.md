# Mixed sensory core preparation

Independent concentration channels prevent a repellent source from becoming food odor merely because an attempt selects an attraction pathway. Both use the existing conservative wall-aware solver. The sensory adapter maps the measured attraction candidate to inhibitory-labeled groups, the avoidance candidate to excitatory-labeled groups, and brightness to vision. Currents add on overlapping sensory cells; motor readout exclusion and separate contact-driven taste remain intact. These are model bindings, not general biological functions of excitation and inhibition.

Verification:

- The environment test compares every cell of mixed sources against each source alone, including leakage through an open doorway and blocking by a closed wall. Forcing both sources into one channel fails on an unexpected concentration of 0.2317604891342925; restoring separate storage passes.
- The attempt test observes exact neuron output for each pathway alone, all together, and each omitted channel, with and without the omitted source still present. Three seeds and repeated 20-tick histories verify deterministic replay. Replacing current addition with overwrite fails on membrane voltage (0.1385366570697123 versus expected 0.3385366570697123); restoring addition passes.
- Rust packed round trips and the real generated TypeScript archive consumer preserve both antenna channels, neural values and terminal absence exactly. The two additional float64 values cost 1,920,000 bytes at 20 flies × 6000 ticks. The full conservative bound is 80,104,384 bytes (76.39 MiB), below the unchanged 128 MiB cap.
- [Diagnostic preservation](diagnostic-preservation.json) compares all six existing real-graph scenarios with baseline commit 859df58: three seeds, both mirrored source positions and 30 updates each. Every recorded pose, neural motor/group value and sensing pose is byte-identical. This preservation check is not a new campaign efficacy claim.
- Real-browser development-route Worker transport passes transfer ownership, hidden credit backpressure, cancellation and deterministic replay. The field browser harness covers both explicit odor channels, reset, lamp/shade, wind and local exit; selecting the repellent fixture displays its recorded values. A deliberate wrong-channel display failed (0.000/0.000 versus 0.532/0.387), then passed after restoration. Initial browser attempts hit an unrelated workbench because two chosen ports were occupied; the successful isolated server used an assigned free port.
- Workspace Rust tests, all-target Clippy, generated TypeScript typecheck, WASM build and 12 client tests pass. The existing ignored actual-graph lifecycle test remains ignored; its separate accepted evidence is unchanged.

Review found one owner for transport, sensory mapping and record layout. Diagnostic overlays consume the active scenario's explicit channel while keeping their single-source presentation. Scientific detail explains the model assumption without describing player controls. No compatibility field or routing fallback remains. An independent read-only agent reviewed the code and all eleven field/detail captures with no actionable finding. The added model-assumption paragraph is visible and readable. CLI independent review is still unavailable as recorded in the root handoff; integration review and affected browser performance/memory gates remain required before accepting mixed-tool attempts.

Preparation covers passes 1–2 only. Placement validation, authored tool resolution, game setup UI and campaign effectiveness remain open, as do all final slice dependencies.
