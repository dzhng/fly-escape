# Bounded fruit and crumb art preparation

Fruit is a recognizable citrus cut surface; crumbs are a distinct scent-only scatter. Both are authored meshes within catalog placement footprints and at most 0.005 world units above the floor. The production renderer hides the old diagnostic food circles and contact pin; diagnostic fixtures retain them by default. No collider, simulation contact radius, recorded position, heading, or lighting changed.

The baseline and candidate use the same deterministic production setup seed 42, camera, geometry, model and placement harness. Baseline was a temporary local counterfactual: hide the food mesh group, restore fruit/crumb generic cylinders, and show old food contact bounds. The source was restored before committing. Comparison telemetry covers all three full/crop pairs; pixel distance measures changed representation, not acceptance. Those paired captures precede authored-house integration. The separate production set exercises the built route after house integration and the common readiness gate; it is not a frozen-input counterpart to the earlier baseline.

Blender MCP authored and exported the preserved fruit and crumbs scenes. `assets/food/roundtrip.json` records reimported geometry bounds and counts. The shared GLB loader validates finite native bounds, mesh budget, and shallow relief. Resource tests establish that placement edits do not dispose shared meshes and replacement/disposal release them once. Removing replacement disposal deliberately makes the resource test fail; restoring it passes. Renderer tests: 19 passed, 136 assertions; TypeScript and production web build passed. Asset-lab valid replacement restores exact canvas bytes; an invalid raised fly asset is rejected with the previous food retained. The rejection capture scrolls the workbench controls to include its error text.

The food-context fixture uses real recorded lifecycle positions and mode transitions. It scales the diagnostic art footprint by the catalog's visual/contact-radius ratio, without altering that recorded simulation. All seven sampled feeding phases have byte-identical pause/reverse restoration. This establishes attached grounded feeding at a recorded contact boundary, not literal mouth intersection: the body-contact rule can begin feeding at the edge while the head faces bare floor. No heading correction or fake collision was introduced.

Fresh visual review was performed by the house renderer agent, which had prior house/motion review context but had not authored or read the food implementation. It inspected all candidate, workbench, and context full frames/crops and the sequence strip. No blocking asset defect was identified within the declared body-contact scope. Remaining visible limits: crumbs are tiny specks in Overview; the yellow selection ring has weak contrast on orange; the recorded edge pose does not establish mouth-to-food contact. The implementer's own visual inspection was informed, not unprimed.

The common world-asset readiness harness delays and fails fruit, crumbs, and fly requests independently. Setup stays disabled until ready; playback remains at tick 0 through a 1.2 second delay; failed playback remains explicit. The inherited house harness verifies house failure, loading, retry, and stale-view behavior through the same gate. These HTTP tests target Vite dev because production can inline small GLBs. The production food harness separately verifies the built setup/playback route with no page errors.

The independent local Codex source-review command could not run: the installed CLI rejects the selected model and asks for a newer version. Root performed source review and requested the common readiness gate. Slice 14 remains prepared rather than fully accepted: later tool shapes and the overall house/art dependencies remain separate work, and literal mouth contact is explicitly outside this body's-contact approximation.
