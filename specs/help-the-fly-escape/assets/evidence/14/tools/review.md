# Remaining floor tool art

Prepared scope: authored vinegar, fan, lamp and shade under the existing non-solid placement contract. Separate Blender scenes, .blend sources and GLB roundtrip results are preserved. All four remain within native radius one and Y=0…0.005, with no collision, physics, sensory mapping, fly height, scene-light or room-material changes. The shade tile represents a visual-cue source; it does not claim a physical canopy or computed shadow.

Production and asset-lab now consume one complete catalog-to-URL registry and one shared PlacementModels owner. The registry is exhaustive against the core ToolKind union. Templates own shared geometry/material resources; placements own transforms. The common world readiness gate requires all six source assets plus house and fly. Loading/failure tests independently cover all six source kinds and fly; setup Run stays disabled and cold playback remains at tick zero through a 1.2 second delayed asset. Failed asset-lab loads remain visible after peers settle. These request interception tests use Vite dev, where files are HTTP resources; production may inline small GLBs.

The actual setup fixture supplies all four tested tools and validates each placement through the core. Both candidate and baseline use seed 42, the same legal placements, model, geometry, camera, and tick 50 record. Baseline is an explicitly temporary counterfactual using the old cylinders and raised fan arrow; it skips installing the four authored source meshes and disposes their parsed resources. The source is restored after capture. No debug flag survives. Comparison telemetry covers both full frames and tight crops; pixel distance is descriptive and does not establish correctness. The independent production build run uses the same browser harness and passes exact canvas pause/reverse identity with no page errors.

The workbench contains diagnostic under-fly and beside-fly positions, not simulated movement. All eight full frames and eight crops are provided for body clearance and isolated shape readability. Existing food replacement tests pass through the renamed shared seam: valid replacement preserves exact pixels, invalid raised/animated fly replacement leaves the accepted food. Twelve focused renderer tests pass with 45 assertions, including all six resource lifetimes. The fan's embedded +X chevron rotates with the core heading and restores exactly after reversal; deliberately reversing the placement rotation sign makes its regression fail, and restoration passes. TypeScript and both production builds pass.

Initial fresh image review came from the house agent, with prior house/food context but without reading or authoring tool source. It inspected every workbench full/crop and actual setup/playback full/crop. It found stable floor contact and clear fly bodies, fan direction and shade distinction. It identified the original radial yellow lamp as confusable with citrus and noted that its production placement hid behind the solid prop. Those captures are retained under rejected-*.

The lamp was reauthored as a square metal housing, pale lens and four fasteners. The test placement moved to a visible core-valid floor location, and the paired counterfactual was recreated there. Vinegar still needs its visible label to identify the particular liquid; the shape establishes a liquid saucer rather than a bottle. The fan keeps both an authored chevron and existing directional arrow, which are conspicuous close but did not obscure the fly in the first review. These are presentation choices, not evidence of biological cue effectiveness.

Root source review confirmed shared ownership/catalog scaling and requested the exhaustive registry, which is implemented. Local codex review --uncommitted could not run because the installed CLI rejects the selected model and requires an upgrade. The final revised review inspected all eight workbench fulls and eight crops plus actual setup/playback fulls and crops. It found no blocking visible defect: the rectangular bright lamp differs from citrus, fly bodies/legs remain clear, and the complete lamp is now visible in the actual fixture. Shade remains distinct and fan direction clear. The reviewer then inspected all four built-production full/crop captures and reported the same verdict, with all four markers visible and no new clipping or depth defect. The reviewer retains prior house/food/first-candidate context; the implementer’s own inspection is not independent. This checkpoint remains prepared for root integration; campaign availability and measured cue acceptance remain outside this art pass.

Root integration at f07d212 passes TypeScript, all 30 renderer tests (180 assertions), and both production builds. The seven independent asset failure/delay cases pass again against the merged development server; the actual built production placement run preserves exact paused/reverse canvas output with no page errors. Reports are retained as integrated-readiness.json and integrated-production.json. The initial attempt found the previous development server stopped; starting a fresh server resolved that environmental prerequisite without source changes. Preview review opened at 2026-09-07 02:10 UTC with production setup/playback and the four close tool views; the decision window remains open.

The nonblocking Preview window closed at 02:15 UTC after about five minutes without new feedback. Root accepts the authored source silhouettes and shared readiness/resource contract on the integrated functional and independent visual evidence. All six floor sources are complete; this does not accept campaign cue effectiveness or difficulty.
