# Placement core/API preparation

The placement owner validates fixed inventory and geometry, applies atomic edits, and resolves a setup into the existing field and body input types. The shared catalog has no threat entry because that circuit gate remains open. Tool values are provisional campaign calibration, not a claim of puzzle effectiveness. Placement-only reserved regions protect prop interiors; physical collision remains owned by level geometry.

Native verification covers placement/move/removal and refunds, unavailable/exhausted tools, walls, outside floor, spawn, reserved props, exit and overlapping tool footprints. Resolution is identical across input list order and preserves food odor when replenishment is disabled. A synthetic neural integration fixture verifies that resolved fruit supplies taste, that rotating a fan removes its local body drift without changing neural heading, and that replay preserves the same 20-tick history. It is a numerical contract test, not evidence of real-fly foraging.

Falsification: disabling inventory decrement fails the remaining-count assertion (1 versus 0); removing spawn protection accepts a forbidden placement; removing the fruit contact region fails both resolution and measured neural taste output. Restoring each passes. The real-browser WASM boundary test also fails if the session ignores request placements: metadata loses its sources, food, fan and inventory consumption. Restoring the binding passes.

[Browser boundary evidence](placement-boundary.json) exercises the actual WASM catalog/edit/resolve functions and a real-graph attempt. Invalid spawn placement is rejected, removing fruit refunds it, resolved metadata agrees with editing, packed data records local wind (0.2604166666666667 along X at the sampled start cell), and repeated attempts produce identical first frames. No application UI, camera or renderer changed. This test uses a blank development-origin browser page to isolate the API without another running lab.

The six real-graph diagnostic trace hashes still exactly match the pre-migration baseline after local-fan and placement integration (same three seeds, two mirrors and thirty updates). Workspace Rust tests, generated TypeScript typecheck, twelve client tests, WASM build and all-target Clippy pass. Self-review checked source ownership, stale consumers, bounds, diagnostics and test mutations. Independent review of the local fan pass passed; a new placement review could not be started because the agent tool returned “agent thread limit reached.” Root integration owns that independent placement review before merging. This is an explicit review limitation, not a completed slice claim.

Next: bind the setup UI and preview to these core inputs, author and calibrate levels, and rerun integrated browser memory/performance gates. Slice 14 remains open.
