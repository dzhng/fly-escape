# First-house seed42 landing failure

The composed pause/rewind/resize check exposed a native motion panic. The Worker previously masked the primary WASM trap with a second error during free; it now clears ownership first and forwards the original error even if cleanup fails. A WASM panic hook prints the Rust location to the developer console.

The captured first-house20-fly request reproduces natively. A zero-time shoe support acquisition is not retained in the trace, so stop_supported unwraps a missing support ID. Retaining that acquisition removes the panic but exposes the preceding error: the captured landing state already penetrates neighboring shoe surface13 by67.8micrometres. Do not accept only the unwrap fix or widen the contact allowance. Investigation must find the prior invalid motion step.

The meadow uses playback time now; pause/reverse frame identities pass until the independent producer fails. Full composed resize acceptance remains red until this motion failure is resolved. The case is assigned to the native-contact implementer while zapper presentation integrates separately.
