# Household integration — captured failure and recovery

The two production houses load native shoes/cat and additional laundry/dishes in the second house. The object list remains visible with editable objects hidden. Native geometry/edibility tests and all 47 renderer tests pass; the merged full sim suite passes with one existing ignored test. The obsolete prepared-corner route test is removed under the user's rejected multi-fan direction.

The actual second-house 20-fly attempt fails with `numerical support unresolved: endpoint blocked by neighboring surface`, seed 18065457143613761157, empty editable setup. The exact request is in failed-attempt.json under attempt.input. This is a red gameplay gate despite green isolated contact tests. Fix ordinary blocked contact as valid collision behavior; do not remove geometry, move the object out of reach, change precision or suppress arbitrary numerical failures.

Adversarial visual findings: at whole-house scale, shoes are hard to identify and the cat resembles a small striped bun; laundry reads as folded linens rather than a garment. The list helps identify fixed objects but does not fix those silhouettes. Expanding the list pushes Run below the visible sidebar on level 2; keep the primary action reachable without hunting. Full fly-follow framing is clear, but these shots do not establish final household realism. Fresh critique is unavailable at the agent limit; final visual acceptance remains open.

## Recovery

Commit07a68ac resolves the recorded neighboring-shoe contact with a verified moving prefix and stationary remainder. Genuine numerical errors remain errors. The same production request then completed at tick302 with all20flies starved, zero underruns and no page errors. This establishes collision recovery, not a useful puzzle solution. Fixed clutter remains in both houses. Source art still has the silhouette limitations above.

The current object tray uses renders of the actual models. The native fan and vinegar bottle also supply nonedible contact geometry. Rotation and hiding controls are gone; “Put objects away” retains earned stars. The primary release action precedes the optional household inventory, addressing its previous offscreen position.
