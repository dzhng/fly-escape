# Landing-root continuity repair

A second-house batch completed five attempts before seed11716541513791412824 aborted with `landing would jump to a different support root`. The captured request and before/after reports are preserved. A descending fly grazes a near-vertical shoe rim; downward support projection returns a root140nm from the verified impact, beyond the existing coincidence criterion. This is an unusable landing candidate, not invalid input.

The landing owner now acquires support only when its projected root coincides with the verified impact under the existing criterion. Otherwise it uses the existing blocking-contact tail, retaining the verified impact and spending the remaining tick there. Missing support also declines that candidate; native query errors still propagate. No tolerance, work budget, geometry, neural output or public flag changes.

Claude produced a reduced red/green regression and native reproduction. Root reviewed the change, reran66 native lib/body/food/placement tests, rebuilt WASM/web and replayed the captured seed through normal production setup. Native and browser both complete at tick310 with20starved; browser has zero underruns and returns to one Worker/canvas. This fixes the recorded abort, not campaign balance or all contact errors.

Further evidence: temporary dense checks found175 declined landings on the repaired run, with zero penetration. The older query-budget seed contains a pre-existing0.156mm worst interpolated penetration, unchanged by this branch. That finding is not evidence of successful exact continuous contact. Subsequent campaign placement tests expose other contact failures; those remain the next reliability work alongside mechanics calibration.
