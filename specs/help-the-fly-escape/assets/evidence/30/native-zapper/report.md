# Native appliance contact hazard

Whole-appliance Zapper contact is an explicit game rule. Contact roles bind stable
edge-connected native surfaces. The verified shared-baker result matches the
supplied scratch JSON exactly after parsing; 41 components, 1132 triangles.
No light or odor attraction is asserted. The production attempt prepares the
validated role table before spawning bodies. Missing, edible, duplicate and
oversized role lists are rejected; existing surface limits remain unchanged.

A native cast records the actual impact fraction and consumes the remainder at
that pose. Existing terminal arbitration retains earlier starvation or escape.
Initial contact and support acquisition are lethal. The diagnostic circle
zappers remain separate and unchanged.

Focused checks cover walking/flight impact and fraction, initial touching,
landing acquisition, nonfeeding, frozen terminal state, safe lateral misses
inside the placement footprint, role validation and earlier starvation.
An exact hull cast at 0.8 m plus high-pose contacts proves clear air above the
0.722m model. This is a native collision query test, not a full body overflight:
the existing body always targets its 0.6 m cruise ceiling. No flight policy changed.

The cast regression was falsified by temporarily removing hazard cast handling;
it failed on the expected outcome assertion, then passed after restoration.
Applicable Rust lib/body/food/placement suites passed 55 tests; workspace all-target
check passed. Runtime/renderer integration and campaign acceptance remain root-owned.

## Production integration

The second-house fixed object loads through the shared actual-model registry and exact native-contact validator. The browser gate verifies nonempty hazard roles reference the resolved physical surfaces, while the player inventory remains independent. Typecheck,48renderer tests and production/WASM builds pass. The device is in the hall, leaving an alternate path and clear spawn/exit regions.

Adversarial visual fallback: the device's violet tubes and grille read at close view, but a faint cutaway wall/header partly overlaps the silhouette. At whole-house view it is small; its native72cm height is appropriate and closer zoom remains available. This is a valid visible integration, not final photorealistic acceptance. No fresh critique slot was available. Full house, close and enlarged device crop are preserved.

A separate first-house seed42 motion failure remains under repair; the zapper's isolated physical and authored-house load gates do not establish overall campaign reliability.
