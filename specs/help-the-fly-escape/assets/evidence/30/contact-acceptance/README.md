# Contact acceptance and complete placement pilot

The motion owner must distinguish an invalid starting pose from a proposed move that collides. A cast can miss overlap when a hull already touches a prop. Candidate endpoints are therefore checked before acceptance; blocked motion retains a checked prefix or the validated starting pose. A blocked departure preserves its support until a complete departure is accepted. Containment is a typed physical result, while malformed inputs and native query failures remain errors. Existing geometry, precision and work limits stay unchanged. Neural behavior and initialization are unchanged in production.

Root reviewed and integrated the four-file repair, corrected two misleading comments, and passed77 focused native tests. Five new regressions cover the actual cat/shoe requests, rejected departure, missing tilted support and invalid original containment. The production WASM, web and asset-lab builds pass. Four captured attempts pass through normal1× hardware-rendered Chrome playback: first-house seed105 and the three prior second-house failures. Each records20 terminal outcomes, zero underruns, measured17ms frame p95, one Worker/canvas and no browser errors. The three second-house runs still end with20 starved; reliability is not solvability. Frame maxima include stalls over300ms, so these focused runs do not establish final release smoothness or input latency.

## Complete six-seed mechanics pilot

All48 actual first-house cells now complete:20 flies, seeds100–105, four fixed placement plans, cold or60 unstimulated neural updates before gameplay. The original input/protocol is preserved in the adjacent campaign-warmup evidence. Root checked unique cells, all terminal outcomes and paired arithmetic in [paired-check.txt](paired-check.txt).

| Initialization | Empty | Apple | Vinegar | Both |
| --- | ---: | ---: | ---: | ---: |
| Cold | 0.50 | 1.00 | 2.33 | 2.33 |
| Warm60 | 1.33 | 2.00 | 3.33 | 4.00 |

Values are mean escapes out of20. This small pilot supports testing warmup and placement benefits on fresh seeds; it does not accept either level. Only one fly-life fed in each initialization arm. The warm apple fly ate nine times and reached the800-tick horizon, so nine meals are not nine independently successful food encounters.

The [Claude report](claude-report.md) provides implementation evidence, with these limits: matching escape counts and completion ticks on35 previously completed cells is not proof of bit-identical trajectories. Root reproduced those scalar matches only. Sampled interpolation checks do not certify every continuous pose, and the older microscopic interpolation defect remains open. Reported hold counters come from temporary instrumentation; the full instrumented source was not retained. The fresh-seed run must preserve its complete executable inputs and sources. No further rerun of this pilot is needed merely to reconfirm its arithmetic.

Next: seeds200–229 with unchanged plans/content, cold versus warm60, preserving every result. Keep initialization experimental until effects generalize. Feeding encounter reliability, two-level calibration and final release gates remain open.
