# Spider-web integration

The first house contains a native web in the study corner. The same repaired authored GLB supplies visible strands and core contact. Actual contact yields caught; genuine gaps remain passable. The native contact and cross-language record checks are described in [native evidence](native.md).

The production household-hazards gate loads both houses, verifies each hazard's kind and resolved surface identity, and keeps fixed hazards outside editable inventory. Both scenes load without browser errors. Typecheck,49 renderer tests, campaign/progress tests and production builds pass. The shared actual-model thumbnail registry includes the web, and caught is counted with the other recorded outcomes.

Production [whole-house](web-house.png) and [closer framing](web-close.png) show the web near the study corner. Against the preceding production frame, the unchanged floor crop changes275 of2080pixels when the web is added; [before](before-crop.png) and [after](after-crop.png) preserve that comparison. It is small and subtle at whole-house scale; closer zoom exposes the spider and strands. This is integration evidence, not final realism or campaign-balance acceptance.

The independent asset review identifies regular polygon rings, stiff spider legs and missing attachment strands as remaining art defects. These do not justify changing contact geometry invisibly.

Independent production review confirms basic presence and recognition at closer framing, but does not accept strong gameplay readability: strands become dotted pixels against wood, the spider collapses to a dark fleck at normal framing, and attachment to the cutaway walls is unclear. Visual refinement remains open.

The final composed production replay with the web present finishes with two escaped, eighteen starved and zero caught, matching the prior seed's outcome. It has zero errors/underruns and identical pause/reverse/resize pixels; [replay evidence](replay.json) records the full result. This proves integration stability, not that the web meaningfully affects this route.
