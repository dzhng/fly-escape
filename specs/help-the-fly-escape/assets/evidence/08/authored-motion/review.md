# Authored motion refinement

The target is recognizable in-place walking, wing cycling, landing deployment and feeding reach, with the accepted static silhouette and grounded scale preserved. World translation remains recorded simulation data. Food contact needs the real food context in slice 15; a bare workbench demonstrates mouthpart action, not eating success.

Blender MCP ran `assets/fly/author.py` then `assets/fly/animate.py` in isolated appended/new scenes, preserving other open scenes. The source now gives the alternating walking tripods a larger sweep, coordinates head lowering with forward/down mouthpart extension, and retains leg deployment long enough to read before landing settles. Both exported assets accompany the authoring script. No palette, material, topology, physics, root translation or clip-duration changes.

The [geometry probe](geometry.json) samples each exported clip at 101 phases through the real loader and skinned vertices: static maximum span remains 0.31056; maximum animated span is 0.31117. Grounded minima stay above -1.5e-8 world units; Fly keeps folded-limb clearance. Three real-asset tests pass, including unchanged clone isolation, finite bounds, loop equivalence, reverse sampling and stationary root. The [browser report](report.json) proves exact PNG pause/reverse restoration for every clip and a running Fly pause, with no browser errors. The worktree used the parent's already-verified phase harness and workbench ordering correction; those are intentionally excluded from this asset commit.

[Comparison telemetry](comparison/visual-parity-diff.json) against the prior peak-feed captures shows both static frames remain byte/pixel identical. Full-frame pixel mismatch is 0.177% for Walk at .125 and 0.072% for Feed at .5: small whole-page values because only anatomy moves. Fly's whole-frame comparison also includes the parent's independent airborne-ring correction and is not evidence of changed Fly curves. The [Walk](sequence-Walk.jpg), [Fly](sequence-Fly.jpg), [Land](sequence-Land.jpg), [Feed](sequence-Feed.jpg) and [Static](sequence-Static.jpg) strips carry the relevant phase evidence; full frames and individual crops remain beside them. The candidate is less wrong for walking/feeding readability and landing deployment.

An independent image-only reviewer inspected all five strips, all 35 sequence full frames and all 35 crops, plus ten browser gate frames. High-confidence observations: clear alternating leg sweeps, conspicuous head-down/mouth extension during feeding, repeated wing silhouette changes, and Land settling into Static by .625. No definite detached parts, tearing or exploding deformation. Remaining medium-confidence limits: Fly can read as flapping near the floor and Land lacks unmistakable descent in the stationary viewer; the featureless floor cannot establish sliding or penetration visually; the mouthpart reads as a rigid dark probe. The numerical floor probe covers penetration, while actual descent/touchdown and food-contact readability remain integrated playback gates. Sparse frames do not prove biological cadence or temporal smoothness.

Review closeout: shape retains one curve owner and shares the landing envelope and feeding reach; diff review found no stale paths or changed consumers; docs point to evidence rather than duplicate curve constants. The independent `codex review --uncommitted` attempt could not run: the configured gpt-6-astra model requires a newer CLI. This is a tooling limitation, not a clean independent code-review result. Root coordinates the non-blocking Preview checkpoint for these five strips before integrated acceptance.
