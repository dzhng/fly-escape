# Recorded motion context

The browser harness replays actual `BrainClient` lifecycle results (seed 6, `mealThenStarvation`) through the authored model and shared height/clip policy. No motor inputs, positions, modes, physics, graph, or WASM were substituted. The captured source record includes build/graph identities. This is a focused diagnostic scene with production renderer contacts, selection ring and existing field marker; it is not a screenshot of the shipped attempt interface. Its transition scan mirrors the latest recorded mode contract because lifecycle records are decoded objects rather than packed attempt chunks.

`sequence-land-{before,after}.jpg` samples the uninterrupted transition at tick 66 through tick 74, including the preceding fractional flying frame. Before uses the former immediate grounded height; after uses the shared descent policy. `sequence-interrupted-land-*` samples tick 44 onward: feeding at tick 50 interrupts the landing and grounds presentation immediately. `sequence-feed-*` samples the actual feeding onset and mouth cycle. Each strip has corresponding full PNGs and unscaled crops. The diagnostic camera follows the fly, so floor/marker displacement is the primary height cue.

The implementer inspected the landing and feeding strips and representative full/crop captures. This is **primed self-review**, not independent visual acceptance. Land remains attached and visibly approaches the floor. The uninterrupted phase reaches height zero when Walk resumes. Feed is grounded with a visible mouthpart reach, but the mouth points beyond the green food disc while the rear body overlaps it. Body contact therefore does not establish convincing mouth-to-food contact in this fixture. This remains an open context defect; changing recorded heading, steering, physics, or art to conceal it is outside this checkpoint. Interrupted landing and takeoff retain documented immediate cuts.

Verification: focused height/clip tests pass (3 tests, 26 assertions); deleting the feeding guard makes the food-grounding regression fail with height 0.6 rather than 0. Typecheck and renderer/client suites pass (27 tests, 224 assertions). The diagnostic verifies exact canvas equality across pause and backwards seek for all three phases; `report.json` records the rendered states and no page errors. The actual playback page separately passed pause/reverse canvas equality in [context-playback](../context-playback/report.json).

Manual source review found a single shared height owner and no simulation changes. `codex review --uncommitted` was attempted but could not run because the installed CLI rejects the configured `gpt-6-astra` model as requiring a newer CLI. Independent source and visual reviews remain open gates for the integrating task.
