# 15 — Level 1 is the tutorial

Status: planned. Dependencies: 03,04,09,12,14.

## Contract and seam

The first complete five-room game teaches placement through an easy, winnable layout.

Authored LevelDef + inventory/star thresholds → complete attempt loop.

## Runnable review surface

Open Window playable from fresh progress; topology diagram, reference/poor placements and paired seed report.

## Verification

Meet GAMEPLAY.md campaign criteria on tuning and held-out seeds. Verify pantry is a genuine one-door dead end and main route has physical reachable exit. No tutorial screen. Complete Run→results→retry→replay. Whole-frame composition check uses previously accepted visual components, then fresh critique.

Visual variable and crop: Integration of already accepted components; whole frame at default/follow and active UI states. New art directions are out of scope; defects return to their owning visual slice.

Use [compare-screenshots](../../../.agents/skills/compare-screenshots/SKILL.md) for the named reference/prior look, recording telemetry and a less-wrong verdict rather than exact pixel matching. Run an unprimed [screenshot-critique](../../../.agents/skills/screenshot-critique/SKILL.md) on all candidate captures and tight crops as the **last visual check before acceptance**. Fix actionable findings and repeat that final check.

Human checkpoint is non-blocking: open shots with [preview-shots](../../../.agents/skills/preview-shots/SKILL.md), allow about five minutes for feedback while continuing independent work, then decide from evidence if silent. Record the decision/rationale and close the opened shots. Silence is not approval for new scope.

## Decision budget

Delegated: Layout dimensions, item counts, generous time limit and positive increasing star thresholds within acceptance criteria.

Human feedback that changes this slice: If the first puzzle is confusing or punishing, simplify its route/inventory rather than adding a tutorial.

Must stay green: all accepted dependency contracts and their focused fixtures; neural motor ownership, real-data provenance, bounded work and the [global invariants](../README.md#standing-gates-and-ownership). Do not broaden testing without a new failure or changed consumer. Record evidence under `assets/evidence/15/` and update the README handoff before ending the pass. Unlisted material decisions require a spec update.

Food composition gate: use the dedicated 14 food-art subpass to replace diagnostic green discs with recognizable edible surfaces. Inspect actual recorded eating at the contact boundary; do not disguise a visual mismatch by changing neural headings or motion. This owns the remaining food-context integration from 08, whose attached pose and landing descent are already verified.
Level 1 fruit/crumb shape preparation belongs to 14. Fruit is a near-flush non-solid edible surface; crumbs supply scent without food. Recorded feeding is initiated from neural readout plus body-circle contact, which may occur beyond the drawn fruit footprint. Integration must verify recognizable food and grounded attached feeding, while disclosing that literal mouth intersection is not guaranteed; do not add steering or change physics to conceal edge contact.

## Bounded native content calibration

Prepare one authorable Open Window JSON using existing LevelDef/AttemptTuning and legal reference/poor placements. The core Attempt with the real graph is the only simulation owner. First compare three tuning seeds; inspect per-fly outcomes, sampled positions and stalled movement before increasing the run count. Geometry, inventory, reserve/time and documented field/sensory/body parameters may change; no steering, motor injection or graph/source-group changes. Taste gain is explicit. Freeze content, placements and positive thresholds before the disjoint 30-seed held-out evaluation. Native calibration does not close browser performance, UI, art or human route-review gates.

Native preparation checkpoint: [exploratory evidence](../assets/evidence/15/README.md) preserves failed pilots. Current content remains unfrozen; median placement gain +2 fails the +4 feasibility target. Next bounded pass fixes geometry/placements and tests spawn heading 0.25 (then 0.325 only if auto-success), on the same three tuning seeds with empty controls. No held-out or full acceptance batch yet.

The 0.25 heading pilot raised empty-control escapes to 11/10/8 and yielded median reference-minus-poor −1. The authorized intermediate 0.325 bracket now tests whether reduced wall trapping can retain a positive placement effect; geometry and both placement arrangements remain fixed.

### Motor-gain diagnostic before further calibration

Heading-only tuning has not produced a robust placement effect. Extend the native probe with bounded actual neural motor/heading samples at stalled boundaries and aggregate absolute turn/delta statistics. First remeasure the compact 0.4-heading, turnGain 2 baseline on one tuning seed. If neural asymmetry is small rather than saturated, test existing turnGain 8 on the same fixed geometry/placements and three tuning seeds with empty controls. No injected steering, reflection, noise or sensory gain above the validated limit of three. This remains exploratory; no 30-seed run until feasible.

Motor diagnostic result: gain 8 increased measured heading response and reduced some boundary stalls, but reference escapes 5/5/4 versus poor 7/5/6 and empty 6/8/6 fail the placement-effect gate (median −2). Pause blind parameter search; next measure actual sensory contrast/timing against the accepted ahead-cue conditions before proposing source sparsity or field settling. Current JSON is a rejected experimental candidate, not app content.

## Startup-transfer diagnostic preparation

Compare the current compact gain-8 content on tuning seeds only, beginning with seed 1000 reference and empty placements. Use actual Graph and Attempt under identical content. The diagnostic condition first advances only fields for 100 game ticks, then warms each brain for 60 ticks at its fixed initial body pose using canonical sensory/contact injection. Body mode, reserve, position and attempt time do not advance during preparation. Fields remain fixed during neural warm-up. Neural noise streams advance with these warm-up steps, so this contrasts complete prepared startup against cold startup rather than isolating field settlement alone. Archive exact temporary patches and source identities; never ship warmed initialization or add a configuration API from this pilot. Expand to three tuning seeds only if a directional placement-benefit signal appears.

[Startup pilot result](../assets/evidence/15/startup/review.md): cold reference/empty 5/6, prepared 1/2; placement benefit stayed −1. No positive signal, so the predeclared expansion was not run and production initialization remains unchanged.
