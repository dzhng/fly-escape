# 27 — Measured sensory sensitivity at anatomical scale

Status: diagnostic contract ready. Dependency: 26. Required before25 physical adoption and20.

## Question and frozen inputs

The corrected sampler measures0.0220–0.0303% relative odor contrast across the authored antennae, below the existing5% cutoff. Test whether a smaller explicit model cutoff restores the already-established sensory-pathway response at this scale. This is a game-model calibration, not a claim about biological detection thresholds. Preserve graph, LIF dynamics, sensory-only injection, gain1, current saturation, motor decoding, grid/source evolution, seed schedule and body trajectory calculation. Do not restore oversized antennae or add a remote-target turn controller.

Use the canonical `cue_currents` owner. In diagnostic builds only, compare the frozen5% baseline with predeclared0.1% and0.01% relative contrast cutoffs. Keep the absolute0.05 intensity floor unchanged. Do not add a player-facing sensitivity control or permanent parallel adapter. No production cutoff changes before the measured verdict. The smallest passing change is preferred only if it passes the behavioral controls; a numerically active input alone does not establish navigation.

## Evidence and decision

First use the26 probe with exactly the same three paired seeds, anatomical coordinates and two initial grid phases. Report actual external currents and downstream motor/body differences for both mirrored odor pathways, neutral fields and bilateral pathway silencing. Uniform nonzero fields must stay neutral; a mirrored analytic gradient must swap the stimulated side. Add brightness/shade probes at the same diagnostic pair to expose effects on the shared visual adapter, explicitly describing this as a planar bilateral approximation rather than compound-eye optics. Preserve absolute intensity-floor rejection and symmetric input rejection.

A candidate needs consistent opposite signed early-turn differences for mirrored odor inputs, loss of those differences with pathway silencing, and preserved opposite lamp/shade orientation under visual ablation controls. Choose based on paired distributions, then run the existing30-seed sensory confirmation protocol at the declared physical sampling coordinates. Retain failed candidates. No campaign seed batches or food tuning here. If shared sensitivity cannot satisfy odor and vision, report the failing pathway and specify separate channel semantics before implementation; do not adjust multiple gains or dynamics until tests happen to pass.

Bank source fingerprints, compact paired summaries and compressed raw evidence under `assets/evidence/27/`. Record exact chosen cutoff and limits in CONTRACTS/choices before production adoption. Anatomical forward/lateral sampling and collision defaults remain25's final adoption step after this verdict. Height remains an explicitly planar approximation; this slice does not validate airborne odor plumes.

Delegated: experiment automation and report formatting. Threshold candidates, controls and frozen inputs are fixed above. This numerical pass has no visual acceptance claim; any introduced visual inherits standing critique gates.
