# 20 — Authoritative furnishing and food contact

Status: in progress. Query replication29, the core query kernel, fixed orientation binding and physical floor height/replay pass. Curved food support and20-brain integration remain open. Dependencies: 19,25,26 and the measured physical-adapter verdict.

## Contract, seam and review surface

Make visible furniture and three-dimensional food agree with body/contact geometry. Core solid footprints remain the only furniture collision/field/placement owner; add appearance identity to that owner instead of inventing a parallel furniture geometry map. Decorative wall detail cannot imply a blocked traversable route.

Food needs a bounded supported-surface model owned by the body/contact module, with recorded support/contact identity and height consumed by playback. First prove one apple/banana fixture with reachable approach, landing, feeding, departure and revisit. A scalar renderer offset is insufficient if it causes surface penetration or teleportation: resolve the actual support profile/transition before accepting the seam. Keep planar steering and existing neural feeding/takeoff/landing decisions; arbitrary furniture climbing and general six-axis flight are not introduced implicitly.

Produce the exact proposed contact types and neutral prototype first; if the supported surface cannot represent curved fruit honestly, reslice that failed question before detailed assets. Tests pin contact acquisition/loss, support transitions, replay, reserve replenishment and unchanged taste/odor in the replenishment-disabled control. This changes physical content and invalidates affected campaign calibration. Visual variable: contact alignment/transition, judged in tight fly/food crops; photoreal materials are deferred.

## Standing verification and decision budget

Use the production renderer in the existing asset workbench, with a representative room, fixed cameras and recorded attempt. This is a diagnostic fixture, not a third campaign level. Preserve twenty real-connectome flies, neural ownership, deterministic replay and the current performance budgets. Blender renders are authoring evidence; actual browser captures determine acceptance.

For visual evidence, save full frames at default, close follow, further zoom and Overview plus the named crops. Use [compare-screenshots](../../../.agents/skills/compare-screenshots/SKILL.md) against the preceding pass/reference and run an unprimed [screenshot-critique](../../../.agents/skills/screenshot-critique/SKILL.md) as the last visual acceptance check. Open shots through [preview-shots](../../../.agents/skills/preview-shots/SKILL.md); the five-minute feedback window is non-blocking while independent work continues. Record the verdict and close shots.

Delegated: reversible asset composition, implementation naming and measurements needed to resolve this slice's question. Record new physical assumptions before dependent implementation; do not silently change neuron dynamics, world scale, contact semantics or fidelity target. Update the global handoff and bank focused evidence under this slice's number. Deferred variables must remain frozen until their owning slice.

## Owner inspection after physical adoption

`BodyState` records planar pose, native root height, mode, reserve and outcome. Landing is airborne until physical touchdown; the renderer and trails consume recorded height. `Body::contacts` now measures surface distance against the prepared edible triangle scene. Actual supported acquisition/movement and recorded support identity remain the next unresolved seam. The body and replay work must consume the existing surface-query owner, with no renderer-side height workaround.

[29](29-surface-query.md) accepts normalized Parry f64 casts for the prototype: metre inputs/outputs with internal millimetre geometry, no extra clearance. Preserve the curved-mesh departure regression. This does not yet choose the fly support shape or change production contact.

## Implemented query boundary and next gate

`crates/sim/src/surface.rs` owns `ContactSurface`, `ContactHull`, `ContactScene` and `SurfaceHit`. Casts constrain a requested hull translation from its native pivot and support orientation; downward queries identify possible support. Rust exports the surface/hit data to TypeScript. The caller still owns actual landing, walking, feeding and outcomes. [Kernel evidence](../assets/evidence/20/contact-kernel/README.md) includes native/WASM agreement and the production normalization regression.

The [native shape candidate](../assets/evidence/20/contact-shape/README.md) is measured but not adopted. Its upright envelope can leave a visible resting-model gap on a sloped apple; support-normal alignment reduces the measured gap. The [browser pose diagnostic](../assets/evidence/20/contact-pose/README.md) now verifies core orientation binding, follow centring and ring attachment, including sampled animation. Uniform shading cannot establish microscopic foot planting; the hull and dynamic contact remain unaccepted. With [native floor height now recorded](../assets/evidence/20/vertical-motion/README.md), carry support normal and food-contact identity through `BodyState`, packed records and playback. Do not leave the new query boundary as an alternate unused physics path at20 closeout.

## Authoritative vertical motion pass

Before curved food integration, remove renderer-only height entirely. BodyState records native root height; Landing is an airborne body mode entered by the existing neural landing signal. It retains flight steering/cost during descent, cannot taste or feed, and becomes Walking only on reaching support. Ground dwell begins at touchdown. Preserve the existing presentation's0.6m cruise height and0.8s full descent as an explicit provisional body envelope: bounded linear vertical speed0.75m/s for both ascent and descent, no instantaneous takeoff. These are modeled game units, not measured biology. Initial flying spawns begin at cruise height; terminal states freeze the actual reached height. This changes the physical timing of landing/feeding, so prior flat decoder feeding timing is not its acceptance oracle.

Packed records and all pose/trail consumers carry core height. Recorded Landing selects the landing clip; a later walking sample no longer invents descent. Prove delayed taste/feeding, touchdown dwell, terminal freeze and exact packed/native/client replay before adding curved surfaces through this same state. No new neural stimulus, steering gain, global calibration or furniture-overflight behavior is authorized by this pass.

The unchanged seed6 meal diagnostic failed after physical descent: landing began at tick44 inside its1.2m food disk, but touchdown at51 was outside it. Preserve that failed record. Increase only this controlled floor-food patch to1.5m: the diagnostic's bounded horizontal speed is0.34m/s (clamped thrust2 × speed0.12 plus wind0.1), so a full0.8s descent needs at most0.272m of extra runway. This is diagnostic content adaptation, not campaign calibration or a new fruit asset scale. Keep seed6, graph, gains and the full meal/contact-loss/starvation plus proboscis-silenced assertions unchanged.

Production comparison exposed a pre-existing native-scale trail defect: the1mm minimum ribbon width became a broad stripe at close zoom (100px in the focused projection regression). Vertical movement also bypassed the old X/Z-only length calculation. Replace both with three-dimensional path length and camera-owned pixel offsets at each endpoint, removing the12mm display lift. Preserve the previous1.5px width target, depth tests, head gap, bounded history and reverse seek. This integration correction is required before accepting the recorded-height pass.

The [authored apple preparation](../assets/evidence/20/authored-apple/README.md) replaces the contact workbench sphere with the Blender GLB and its exact baked triangles. Resolve these through the placement/attempt owner next. Its top indentation clips a normal-aligned planar selection ring; supported replay must conform the ring to the visible surface rather than treating orientation alone as sufficient.

## Remaining reviewable passes

1. **Food geometry and identity — component verified.** Move edible geometry onto the existing `ContactSurface` boundary in authored metre units. One immutable prepared contact scene per attempt must serve every fly; do not rebuild mesh acceleration structures per tick. Visible neutral food and collision queries consume the same authored triangles and stable IDs. Replace the planar food-overlap implementation rather than retaining an alternate controller. Controlled floor-food probes remain flat surfaces in this same query path; zapper/reserved-region circles remain their separate existing concepts. Verify a flat food surface and one curved apple through the real placement/attempt owner before accepting this pass.
2. **Supported movement and recorded state.** Add authoritative support identity/orientation beside native height. Resolve actual hull acquisition, continued walking, feeding, loss of support, departure and revisit from neural steering and existing landing/feeding signals. Preserve a continuous native root path: changing orientation or projecting to support must not teleport or penetrate the visible fruit. Choose the final native hull using these moving cases, not the five fixed poses alone.
3. **Curved playback and integration.** Prevent straight interpolation between supported samples from cutting through curved fruit. Choose one core-owned sampling/path representation and prove its worst-case archive/work bounds before implementation; a second renderer physics implementation is not permitted. Test forward play, pause, reverse seek and support transitions in close/extra-close/context/Overview. Then run twenty real brains and compare ordinary versus replenishment-disabled attempts with identical food, taste, odor and seeds. Only this gate accepts20 and unlocks final furnished content/calibration.

These are checkpoints within20, not smaller substitutes for its original scope. Retain the two-level game and later art, visibility, difficulty and release gates.
