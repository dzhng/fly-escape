# 01 — Reproducible graph and reference

Status: complete. Dependencies: none.

Evidence: [slice review](../assets/evidence/01/review.md), real graph comparison and source hashes.

## Contract and seam

Restore the real dataset and capture a trustworthy oracle before replacing Python.

Offline exporter → graph binary + manifest + injected-noise fixtures; graph contract in CONTRACTS.md.

## Runnable review surface

A graph report with source hashes, group coverage, extraction parameters and reproducible commands.

## Verification

Reproduce the current Python extraction on actual data; compare signed asymmetric edges and motor groups. Two clean exports must match bytes. Record the deliberate stable-order correction to MBON truncation. Validate all required groups are nonempty. Pin LIF ordering with deterministic injected noise; label miniature synthetic fixtures clearly. Missing remote data blocks real-graph acceptance, while fixture and UI work can proceed.

## Decision budget

Delegated: Reuse, rewrite or delete spike tooling according to the [spike reuse policy](../CONTRACTS.md#existing-spike-code), retaining only useful reference evidence. Download mechanics, compression and binary field order; no silent switch to significant-only weights or induced subgraph.

Human feedback that changes this slice: Missing groups, unreasonable download size or failed reproduction change the extraction decision; do not invent data.

Must stay green: all accepted dependency contracts and their focused fixtures; neural motor ownership, real-data provenance, bounded work and the [global invariants](../README.md#standing-gates-and-ownership). Do not broaden testing without a new failure or changed consumer. Record evidence under `assets/evidence/01/` and update the README handoff before ending the pass. Unlisted material decisions require a spec update.
