# Kitchen run

The [catalogue](../catalog.json) owns one combined footprint for the appliances and counter. A continuous plinth matches that solid; the sink and appliance details do not introduce independent physics. The grounded centre origin uses +Z for the working front.

[author.py](author.py) owns native geometry and materials and validates export bounds against the catalogue. Leaving space above the counter allows an independently mounted wall window to remain visible.
